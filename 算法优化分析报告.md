# NE-Memory 算法优化分析报告

> 分析日期：2026-08-01 · 只读审计，未修改任何代码
> 覆盖范围：`src/core/vault/*`（存储层）、`src/core/engine/*`（管线/检索/注入）、`src/core/api/llm.js`、`src/core/tools.js` 等全部核心模块（第一轮）；`src/adapter/panel-*.js` 全部 12 个 UI 面板 + style.css + 入口/初始化文件（第二轮 UI 专项），共 50+ 源文件。
> 结论均基于源码逐行阅读 + 调用链 grep 交叉验证。

---

## 〇、修复状态追踪（持续更新）

> **约定**：本表是修复进度的唯一真源。每修复一项，立即更新本表 + CHANGELOG.md 顶部 Unreleased 块；本表条目从"未修复"变为"已修复"时保留修复方式、涉及文件与验证结果。

| 编号 | 问题 | 状态 | 修复方式 | 涉及文件 | 验证 |
|---|---|---|---|---|---|
| P0-1 | access 工具 `msgId` 未声明 → ReferenceError | ✅ 已修复 | `[→msgId]` → `[→numId]`（回显 LLM 引用数字）；`findMessageInChat` 新增裸数字 O(1) 下标反问 + id 身份校验，漂移回退全量扫描；守卫 `!msgId` → `msgId == null \|\| msgId === ''`（放行下标 0） | tools.js L47、msg-id.js、msg-id.test.js | 新增 6 条用例，41/41 通过；`npm test` 全绿 |
| UI-1 | panel-settings.js `setRetrievalEnabled` 从未 import → 保存设置 ReferenceError，channels 配置不落盘 | ✅ 已修复 | L7 补 `import { setRetrievalEnabled }` | panel-settings.js | `npm test` 全绿（适配层改动，单测无直接覆盖） |
| UI-2 | embedding 通道 key 取不存在的 `nes_embedding_api_key` → fetch models 永远无鉴权 | ✅ 已修复 | keyId 特判：`prefix === 'nes_embedding'` 时用 `nes_embedding_key` | panel-settings.js | 同上 |
| UI-3 | panel-usage.js 图表默认对象用 `sp`、消费方读 `.state` → State 序列断档 + 误判无数据 | ✅ 已修复 | 默认对象与判定统一为 `state: 0` | panel-usage.js | 同上 |
| UI-4 | customFieldRefs 选择器同时匹配字段名 + 类型两个 span → 保存时把 `'string'/'number'` 混入字段引用并写入字段库（三处保存 + 两处查重同源） | ✅ 已修复 | 选择器改 `> span:first-child`（字段名 span 均为容器首个子元素），5 处一并修正 | panel-state-cards.js L1209/L1387、panel-templates.js L1444/L1491/L1556 | `npm test` 全绿（41/41 msg-id 等 27 文件 + 3 ratchet 通过） |
| UI-7 | panel-content.js 防刷新保护漏 `.ne-inline-row`/`.ne-char-edit` → 编辑中内容被 vault:updated 抹掉（数据丢失） | ✅ 已修复 | 编辑保护 `closest` 条件补两类编辑态 class | panel-content.js | `npm test` 全绿 |
| UI-9 | bootNE 无守卫，DOMContentLoaded + interactive 双触发 → 工具重复注册、vault 双写 | ✅ 已修复 | `window.__ne_booted` 启动守卫 | index.js | `npm test` 全绿 |
| UIB-1 | style.css 整体死代码（manifest css 空、rollup 无 CSS、CDN 只装 index.js）→ 设计 token（`--ne-*`）与 `.narrative_memory_table` 基础样式从未加载，全部静默失效 = "UI 变丑"主因 | ✅ 已修复 | token 块注入 PD.head `:root`（shadow 树经 host 继承）；memory table 基础规则注入 style 主串（含 td word-break）；其余 548 行规则经审计 0 使用为死代码，style.css 清空为废弃占位 | panel-shared.js（injectBottomDrawerCSS）、style.css | `npm run build:all` 成功；`npm test` 全绿 |
| UIB-2 | Shadow DOM 模式 `.ne-mobile` 挂在 host、样式在 shadow 内 → 移动端适配全失效；原 `.ne-mobile .ne-vault-bottom-overlay::before` 两分支均不匹配 | ✅ 已修复 | 动态前缀 `mobileSel`：shadow 用 `:host(.ne-mobile)`、非 shadow 用 `.ne-vault-bottom-overlay.ne-mobile`，三组规则（L3 响应式 / 模板单列 / 双区切换）统一替换 | panel-shared.js（injectBottomDrawerCSS） | 同上 |
| UIP-1 | `vault:updated` 面板全量重建：角色/阵营/任务卡 + STM/LTM 表 + quick index 无条件 innerHTML 重建 + 事件重绑，数据未变也触发解析/重排/失焦（每轮 pipeline 卡顿主因） | ✅ 已修复 | 区块级 HTML 缓存：char/faction/quest 渲染字符串比较、STM/LTM 输入 JSON 签名、quick index 内容比较，全部未变则跳过 DOM 重建与重绑；setTimeout(50) 二次绑定改同步且仅重建时执行；热路径 console.log 改 `__NE_DEV_MODE` 守卫 | panel-content.js、panel-drawer.js | `npm run build:all` 成功；`npm test` 全绿（27 文件 + 3 ratchet） |
| UIP-2 | 模板卡渲染每卡重复 `isDialogueTemplateLocked(_getCurrentCharName(), id)` → 每卡一次 `SillyTavern.getContext()` + `loadCardConfigSync`（localStorage 读 + JSON.parse） | ✅ 已修复 | 基于渲染已读入的 cardConfig 参数内联判断锁定态（新增 `_tplConfigLocked` helper），一次读取全卡复用；`store.js` 的 `isDialogueTemplateLocked` 变无引用导出（ratchet WARNING，待清理） | panel-templates.js | 同上 |
| UIP-3 | 设置页 8 处滑杆 oninput 每 tick 全量保存：6×setItem + neSyncAll 全量扫描（拖动时卡顿） | ✅ 已修复 | `_debouncedSaveSettingsTab()` 300ms 防抖，拖动结束才写入；label 同步保留，onchange（输入框/开关）保持直存 | panel-settings.js | 同上 |
| UIP-4 | 设置保存全量热更新（ST 同款）：任何单项变更都经 saveSettingsTab 全量重写 `ne_settings` + channels 再全量重写 5 份 API 配置 + neSyncAll 全量扫描（改 1 项 = 6 份全量 JSON + 全量同步） | ✅ 已修复 | ①`ne_settings` 写前变更检测（JSON 值比较，未变不落盘不同步）②channels 4 份 API 拆分 `_saveChannelApiOnly` 独立写 + neSync ③embedding/secondary 复用既有拆分（内部已带 neSync）④保存后 neSyncAll → neSync(key) 精确同步（neSyncAll 变无引用导出待清理）；`getModelValue` 保持传 `prefix + '_api'` 与原语义等价 | panel-settings.js | `npm run build:all` 成功；`npm test` 全绿（27 文件 + 3 ratchet） |
| UIS-1 | showConfirm 的 document keydown（Esc）监听只在按 Esc 时移除自身，点确定/取消/遮罩关闭则每次弹窗残留一个 | ✅ 已修复 | close 统一移除 escHandler，任何关闭路径都清理 | panel-shared.js | `npm run build:all` 成功；`npm test` 全绿（27 文件 + 3 ratchet） |
| UIS-2 | showHelpCard 外部点击监听在 X 关闭按钮（inline onclick 直接 remove 卡片）与 hideHelpCard 路径不移除 | ✅ 已修复 | 统一 closeCard：X 按钮 / 外部点击 / hideHelpCard 三路径都 `removeEventListener`（监听句柄挂在 card 上） | panel-shared.js | 同上 |
| UIS-3 | closeVaultOverlay 每次调用 addEventListener transitionend，overlay 已 display:none（无过渡触发）时监听永留累积 | ✅ 已修复 | 命名 handler `_overlayTransitionDone` + 关闭前先移除 + 兜底 600ms timer 重置 | panel-shared.js | 同上 |
| UIS-4 | bootstrap 全局 keydown（卡片回车/空格切换）无去重守卫，init 双跑时累加 | ✅ 已修复 | `_keyNavBound` 守卫，`_bindCardToggleKeyNav()` 只绑一次 | bootstrap.js | 同上 |
| UIS-5 | 移动端 ResizeObserver 存在 overlay 上但 close 时从不 disconnect；init 双跑可能重复创建 | ✅ 已修复 | stopOverlayResizeWatcher 一并 disconnect（closeVaultOverlay 必经路径）；setupMobileObserver 防重复创建 | panel-shared.js、panel-init.js | 同上 |
| UIS-6 | panel-version-history 两个限制滑杆 oninput 每事件写 localStorage + neSync（与 UIP-3 同源） | ✅ 已修复 | oninput 只更新即时显示，onchange（拖动结束）才 saveConfig | panel-version-history.js | 同上 |
| UI-5 | panel-state-cards LTM 主行 title/event/period 未 HTML 转义直接拼 innerHTML（STM 行已转义） | ✅ 已修复 | ltmTitle/ltmEvent/periodCell 统一 escapeHtml | panel-state-cards.js | 同上 |
| UI-6 | writeState 无 catch 静默失败（保存无反馈）；保存后清理逻辑是死代码（`.ne-card-edit-form`/`.ne-card-edit-btns` 从不创建）；deleteCharacterCard 同 | ✅ 已修复 | saveCardFields/deleteCharacterCard 的 writeState 加 catch + toast；死代码替换为显式 exitCardEditMode | panel-state-cards.js | 同上 |
| UIS-7 | showConfirm close 依赖 transitionend 才 resolve：弹窗 CSS transition 被主题/样式覆盖禁用（transition:none 等）时事件永不触发 → Promise 永久挂起，确认后的删除/保存不执行（"点删除没反应"可疑根因，与 UIS-3 同源模式） | ✅ 已修复 | close 加 300ms 超时兜底：transitionend 触发则即时清理，不触发则超时强制移除弹窗 + resolve | panel-shared.js | `npm run build:all` 成功；`npm test` 全绿（27 文件 + 3 ratchet） |
| UI-8 | 消息内假按钮不可点击：`_renderAlertQueue` 把按钮拼 `[文字]` 纯文本进系统消息，ST 当前版本无消息内按钮机制（已本地查证 ST 源码无 messageButtons API）→ 按钮不可点击且文本误导；`triggerPerRoundExtraction` 的"查看模板库"按钮即此 | ✅ 已修复 | 诚实降级：按钮以 `·` 分隔纯文本提示呈现，不再拼 `[文字]` 假按钮；`sendNeInteraction`/`sendNePopup` 接口保留（测试 + 未来降级路径），events.js 调用点改走 `sendNeNotification` | ne-system-msg.js、events.js、ne-system-msg.test.js | `npm run build:all` 成功；`npm test` 全绿（新增 ST 降级路径断言 9/9，28 文件 + 3 ratchet） |
| UI-10 | 初始化窗口期首轮消息漏记：init 先 `_bootstrapVault`（loadVault + 迁移 + 渲染面板，耗时）再绑事件 → 期间到达的首轮 `message_received/sent` 无人监听永久漏记 | ✅ 已修复 | 事件绑定 + 工具注册 + ChatCompletion patch 移到 bootstrap 之前；bootstrap 首次初始化分支加重读保护（写前 readState/readMemory 检查已有数据则跳过全量初始化写，防覆盖竞态窗口已写入消息） | index.js、bootstrap.js | 同上 |
| UI-11 | 设置页控件在 Shadow DOM 下失效：`.ne-manual-controls`/`.ne-adaptive-only` 用裸 `document.querySelectorAll`，扩展模式面板在 shadow root 内查不到 → 手动控制/自适应专属选项无法切换 | ✅ 已修复 | 替换为 shadow-aware 的 `panelQSA`（panel-shared.js 已 import） | panel-settings.js | 同上 |
| UI-12 | FIND_PROMPT 双重转义致提示剥离失效：`\\\\` 级转义经 ST `regexFromString`（`new RegExp`）编译为字面反斜杠，与真实 `\|` 分隔符不匹配 → NE-BANNER/NE-CHAR 块无法剥离、系统提示残留原始格式 | ✅ 已修复 | FIND_PROMPT 降为 `\\` 级与 FIND_PIPE/CHAR_FIND 对齐；新增 `test/banner-regex.test.js` 冒烟测试（内联三正则 + 模拟 ST 编译，验证匹配/剥离/编译不抛错），注册进 test/run.mjs | events.js L836、banner-regex.test.js、run.mjs | `npm run build:all` 成功；`npm test` 全绿（28 文件 11/11 + 3 ratchet） |
| P0-2 | v6→v7 迁移空库永久挂起：`_migrateVaultsToSplit` 的 `verifyHashes.forEach` 空数组回调不执行 → `done >= checks`（0>=0）永不成立 → Promise 永不 settle → `openDB` resolve 不执行，存储层全部 read/write 冻结 | ✅ 已修复 | cursor onsuccess else 分支加空数组早退：`checks === 0` 直接 `resolve(migrated)` 完成迁移（无事可做直接结束） | store.js L49-52 | `npm run build:all` 成功；`npm test` 全绿（28 文件 + 3 ratchet） |
| P0-3 | 队列 rejection 毒化：三个 enqueue 用 `.then(success, rejection)` 双参数链，rejection handler `throw e` → 失败任务之后所有任务只走 rejection 分支、taskFn 永不执行（记忆写入静默丢失），直到 reset() | ✅ 已修复 | 改 `.then(success).catch(failure)` 链式结构：失败经 `addAnomaly('pipeline_task_failed')` + console.error 记录，尾部 catch 吞错保链 resolved；返回 Promise 永远 resolved（await 调用方不再收未处理 rejection）；新增测试：任务 A 抛错后 B/C 仍按序执行 + 队列最终 idle | pipeline-guard.js（三 enqueue）、pipeline-guard.test.js | `npm run build:all` 成功；`npm test` 全绿（pipeline-guard 25/25，28 文件 + 3 ratchet） |
| P0-4 | STM 校验系统性误报：`validateMsgRanges` 用本次增量条数（`filteredMessages.length`）校验 msgRange 的全局绝对消息索引 → 长对话增量更新必报"越界 + 未覆盖"，污染 telemetry validation_warnings | ✅ 已修复 | 窗口语义：`validateMsgRanges`/`validateSTMOutput` 增可选参数 `windowStart`/`windowEnd`（默认 0..messageCount-1 兼容旧签名）；越界检查按窗口边界、covered 数组改连续性检查（首 range 锚定窗口起点、相邻衔接、末 range 锚定窗口终点）；stm-pipeline 调用处传 turns 首末条全局下标；新增 5 个窗口用例（含增量场景零误报） | validate.js、stm-pipeline.js L552、stm-validate.test.js | `npm run build:all` 成功；`npm test` 全绿（stm-validate 25/25，28 文件 + 3 ratchet） |
| P0-5 | 孤立分支机制全链路断链：orphaned_branches 无写入生产者，`restoreBranch`/`cleanupBranches` 无调用方，`pruneOrphanedBranches` 唯一调用（state-pipeline L891）消费的永远为空数据 → 回滚后旧版本物理删除不可恢复，但"恢复已删记忆"与产品原则（记忆生命周期 = 对话生命周期：对话删除 → 记忆物理删除；重 roll/swipe → 回退版本链 + 重新提取跟随版本）矛盾 | ✅ 已移除（经讨论确认方向） | 删除 `restoreBranch`/`cleanupBranches`/`pruneOrphanedBranches` 三函数 + state-pipeline 调用 + 死变量 `BRANCH_TTL_MS`；保留 `orphaned_branches` store 定义（避免 IDB schema 变更）与 `remove(chatId)` 防御性清理；文档同步为"回滚 = 物理删除，记忆随对话生命周期" | state-versions.js、state-pipeline.js | `npm run build:all` 成功；`npm test` 全绿（28 文件 + 3 ratchet，dead-exports 无新死代码） |
| P1-10 | vocabularyOverlap 普通对象当 Set，token `constructor`/`toString`/`valueOf` 命中原型链 → 相似度虚高 | ✅ 已修复 | `Object.create(null)` 无原型对象替代 `{}`（两条 token 集） | text-utils.js | `npm run build:all` 成功；`npm test` 全绿（28 文件 + 3 ratchet） |
| P1-11 | `total_smartpush_injections = total_turns` 占位伪统计，无真实计数来源且无消费方，UI 展示误导 | ✅ 已修复 | 删除字段（含注释示例 + aggregate 初始化） | chat-telemetry.js | 同上 |
| P1-12 | listAllChatIds 漏 `memory_vaults` store → 孤儿 memory 数据永不清理 | ✅ 已修复 | stores 列表补 `'memory_vaults'` | garbage-collector.js | 同上 |
| P1-13 | 字符串时间戳 → `NaN` 一路比较 false → 输出"NaN 个月前" | ✅ 已修复 | 入口 `Number()` 归一化 + 空值/NaN 早退 | time-utils.js | 同上 |
| P1-14 | 派系扫描英文大小写敏感（alias `order` 匹配不到 `Order`） | ✅ 已修复 | 文本与关键词统一 toLowerCase 比较 | state-pipeline.js | 同上 |
| P1-15 | 模式3 `resolved` 存"实体名+后缀"语义不一致；`replace` 无 `/g` 只替换第一处引用 | ✅ 已修复 | 只存实体名（与模式1/2 一致）；`split/join` 全局替换（避免正则转义） | ambiguity.js | 同上 |
| P1-16 | `present_characters[0].scene` 死代码 fallback（present_characters 是字符串数组） | ✅ 已修复 | 删除 fallback，仅 `openLtm.scene \|\| ''` | consolidate.js | 同上 |
| P1-6 | `__proto__`/`constructor`/`prototype` 作为 dot-path 段时，mergeStateChanges 的 `current[key]` 写入/读取落到 `Object.prototype` → 校验绕过 + 原型污染风险 | ✅ 已修复 | schema.js merge 路径遍历 + ensureCharacterTemplate 调用处 + state-versions `_setByPath`/`_getByPath` 统一 `isReservedKey` 拦截 | schema.js、state-versions.js | `npm run build:all` 成功；`npm test` 全绿（28 文件 + 3 ratchet）；新增 `__proto__`/`constructor` 污染防护断言（schema.test.js） |
| P1-7 | 校验缺口三连：resolveSchemaPath 不支持 `item_schema`（abilities/inventory 子结构零校验）；object 类型零校验；required 字段从不校验 | ✅ 已修复 | ① item_schema 步进（动态键丢弃 + 模板匹配，修正模板内 `type` 字段定义冲突）；② validateField 补 object 类型检查；③ validateStateChanges 补 required 空值拦截（拒绝写入 + warning） | schema.js | 同上；新增 item_schema/object/required 用例（schema.test.js，167 passed） |
| P1-8 | merge 层 `__inc` 只对 affection 生效，其他 number 字段的 `+N`/`-N` 被当对象写进状态 | ✅ 已修复 | merge 泛化 `__inc`：任意字段应用增量，affection 保留 0-100 clamp，capturedChanges 记录 old/new | schema.js | 同上；新增非 affection 增减用例 |
| P1-1 | stm-pipeline.js L298-330 超长 segment 非首位置时不拆分（仅 currentChunk 为空时拆），累积场景下超长 chunk 整段提交给 LLM | ✅ 已修复 | 超长 segment 无论位置先 flush 当前 chunk 再 `splitIntraSegment` 拆分；导出 `chunkSegmentsForLLM` 供单测 | stm-pipeline.js、stm-chunking.test.js | `npm run build:all` 成功；`npm test` 全绿（29 文件 + 3 ratchet）；新增拆分回归用例（累积场景不整段提交） |
| P1-2 | stm-pipeline.js L512-517 事件按数组下标与 segment 一一映射，LLM 少输出即全量错位 | ✅ 已修复 | 优先用事件自带 `msgRange`（窗口内下标）→ 经 windowMessages（含 `_absIdx`）转全局下标 → 与 turns 相交得覆盖轮次；两处调用传 filteredMessages；导出 `mapEventData` 供单测 | stm-pipeline.js、stm-chunking.test.js | 同上；新增 msgRange 窗口映射用例 |
| P1-3 | adaptive-context.js L219 `usableBase = Math.max(2000, ...)` 强抬下限，maxContext<2300 时 goldenUpper > 模型容量，压缩永不触发 | ✅ 已修复 | 下限改 1000 + `goldenUpper = Math.min(goldenUpper, usableBase)` 封顶在可用预算内 | adaptive-context.js、adaptive-context.test.js | 同上；新增 Test 18 小模型用例（21/21） |
| P1-4 | llm.js L585-651 AbortError 无条件重试 ×2 轮 × 每轮双请求全部等满 timeoutSec → 单次超时最坏 ~19s+ | ✅ 已修复 | AbortError 不重试直接抛（重试只会等满更多 timeout 且加剧延迟），仅 5xx/网络类重试 | llm.js | 同上 |
| P1-5 | msg-id.js L29-33/L83-114 数字 id 退化格式（`send_date='0000-00-00...'`）消息漂移后 fallback 断链，msg 引用永久丢失 | ✅ 已修复 | `_legacyScan` 提取退化日期段尾段数字 id 按 m.id 匹配（无 id 消息按位置即身份） | msg-id.js、msg-id.test.js | 同上；新增退化日期用例 4 条（48/48） |
| P1-17 | stm-pipeline.js L339 强制 `status='closed'` 覆盖 LLM 的 partial | ✅ 已修复 | 仅缺失/非法值归 closed，partial 语义保留——partial 事件经 `cursor_state.stm.pending_partials` 在下一窗口 prompt 中以 partialCtx 续接（stm-pipeline L39-46 构建"上次未完成事件"上下文），原强制 closed 使跨窗口续接链路永远闭合 | stm-pipeline.js、stm-chunking.test.js | 同上；新增 partial 保留用例 |

---

## 一、总体诊断

### 架构概览（真实调用链）

```
SillyTavern 事件
   │
   ├─ events.js ──► pipeline-guard.js（三队列串行化）
   │                  ├─ executeIncrementalUpdate（stm-pipeline: 切分→LLM→STM 事件）
   │                  ├─ extractStateChangesOnly（state-pipeline: 角色/派系→LLM→state 变更）
   │                  └─ ltm-pipeline（LTM 决策）+ consolidate（applyLtmDecision）
   │
   ├─ injection.js（SmartPush，每次生成时触发）
   │                  └─ retrieval-filter.js（BM25+RRF+向量融合）→ embedding.js
   │
   └─ 持久化：store.js（IndexedDB）→ state-versions.js（增量版本链）
              + chat-telemetry/token-stats（localStorage 全量读写）
```

### 全项目共性模式（最值得治理的三件事）

1. **localStorage 全量 read-modify-write + `neSync()` 高频叠加**：chat-telemetry / token-stats / telemetry / params / llm 的 settings 读取，每一轮对话叠加可达 **10+ 次全量 JSON.parse/stringify**（chat-telemetry 的 `recordChatToken` 一次触发 2 次 `recordChatStat`，每次 1 parse + 1 rebuild + 1 stringify + 1 neSync）。
2. **IndexedDB 无连接缓存**：`openDB()` 每轮 pipeline 被调用 **6-8 次**，每次都是异步 open + 升级检查（store.js + state-versions.js + GC 叠加）。
3. **LLM JSON 解析入口不统一**：safeJsonParse 已接入 4 个 pipeline 文件，但 ambiguity.js / contradiction.js / consolidate.js 仍各自手写贪婪 `match(/\{[\s\S]*\}/)` + `JSON.parse`，且 `/\{[\s\S]*\}/` 贪婪模式在输出含多个 JSON 块时必挂。

### 重大死代码（文档与实现漂移）

| 位置 | 内容 | 状态 |
|---|---|---|
| stm-pipeline.js L13-269 | `buildCursorPrompt` / `buildBatchPrompt` / `buildStmOnlyPrompt`（约 250 行，含 BM25 预分组调用） | 全项目无调用 |
| bm25-grouper.js（整文件） | `preGroupItems` 唯一调用点在上述死代码中 | 生产路径不触发 |
| stm-pipeline.js L271-274 | `segmentTurns` 退化为返回单区间的 stub（CODE_WIKI 的"三级切分"已丢失） | 仅单区间 |
| injection.js L179 + retrieval.js | SmartPush 路径 `entityChains` 恒为 `{}`，`lookupEntityChains` 只在 legacy recall 工具中被调用（**已修复：P1-18 补接线恢复**） | 实体链增强链路已接入 SmartPush |
| state-pipeline.js L782-804 | `tools` 恒为 null，工具分支 + MAX_TOOL_ROUNDS 循环永不执行 | 死代码 |
| state-versions.js 孤立分支机制 | `orphaned_branches` 全代码库无生产者（rollback 直接删 delta 不落分支） | restoreBranch/prune/cleanup 全死代码 |

---

## 二、谬误清单（确定性 bug / 逻辑错误）

按严重程度排序，均给出修复方式与损益。

### P0-1 【致命】tools.js L47：`msgId` 未定义变量 → access 工具 msg 分支必然 ReferenceError

```js
if (ref.indexOf('msg#') === 0 || /^\d+$/.test(ref)) {
    ...
    if (!msg) result = 'Message #' + numId + ' not found.';
    else { ... result = '[→' + msgId + ']\n' + text; }   // ← msgId 从未声明
}
```

- **验证**：全 src 目录仅此一处出现 `msgId`，ESM 严格模式读取未声明标识符抛 ReferenceError，被外层 catch 吞掉后返回 `'Error: msgId is not defined'` 并错误记录 `access_success: false`。任何 `msg#N` 或纯数字引用且消息存在时 **100% 失败**。
- **修复**：`msgId` → `numId`（一行）。
- **损益**：纯收益，零代价。

### P0-2 【致命】store.js L49-91：`_migrateVaultsToSplit` 空 store 时 Promise 永不 resolve → 存储层永久挂起

```js
var checks = verifyHashes.length;   // 空数组时 = 0
var done = 0;
verifyHashes.forEach(function(h) { ... });   // 回调一次不执行，resolve(migrated) 永不被调用
```

- **触发条件**：v7→v8 升级路径下（`localStorage.ne_v7_migrated !== '1'`）且旧 `vaults` store 为空或无带 content 记录时，`openDB()` 在 `_hasOldVaults(db)` 为 true 时 await 此 Promise → 所有 read/write 全部挂起。
- **修复**：`if (checks === 0) { resolve(migrated); return; }` 或改 Promise.all 模式。
- **损益**：纯收益（一次性灾难性挂起的消除），零代价。

### P0-3 【高】pipeline-guard.js L45-93：队列 rejection 传播 → 单任务失败后该队列后续任务被永久跳过

```js
_stateQueue = _stateQueue.then(
    async function() { ...; try { await taskFn(); } finally { ... } },
    async function(e) { _setStatus('state', 'idle'); throw e; }   // ← rethrow 毒化队列
);
```

- **机制**：任务 B 抛错 → 链 rejected → 任务 C 的 `.then(success, rejection)` 只走 rejection 分支（置 idle 后 rethrow）→ C 的 taskFn **从未被调用** → D、E… 全部被跳过，直到有人调 `reset()`。记忆写入静默丢失。
- **现状缓解**：多数调用方在 taskFn 内部 try/catch 兜底（events.js L768/L1274/L1476），但 events.js L483/L557/L672 的入队任务无内部兜底，一旦 `_detectAndRollback` 等抛错即触发。
- **修复**：rejection handler 不 rethrow（记录错误后返回 resolved），失败信息改由返回值/事件暴露。
- **损益**：收益大（消除写入丢失）。代价小——现有调用方多 fire-and-forget，需检查返回值语义的调用方极少。

### P0-4 【高】validate.js L91-117：msgRange 用局部消息数校验全局绝对索引 → 必然误报

```js
// stm-pipeline L552 传入 messageCount = filteredMessages.length（本次新增数）
if (range[0] < 0 || range[1] >= messageCount) { errors.push('stm_entries[i].msgRange 越界...'); }
// covered 数组只有 messageCount 个槽位 → 全局 idx 全部"未覆盖"必报
```

- **后果**：长对话增量更新时（如全局 idx 42-45，本次 4 条），`range[1]=45 ≥ 4` 必报越界，covered 4 槽位全空必报未覆盖 —— **确定性系统性误报**，污染 telemetry 的 validation_warnings，误导开发者以为 LLM 输出有问题。
- **修复**：校验基准改为全局消息总数（`ctx.chat.length`），覆盖检查按全局区间做连续性检查。
- **损益**：纯收益（一处传参修改）。

### P0-5 【高】state-versions.js：孤立分支机制全链路断链 → 回滚后旧版本物理删除不可恢复

- rollbackState/rollbackMemory（L548-550/L588-590）把孤立 seq 的 delta **直接 delete**，从不写入 `orphaned_branches`；而该 store 全代码库无任何 put（仅测试手动模拟）。restoreBranch（L650-671）恢复的是已不存在的 delta → 恢复结果错误或为空。
- **修复**：rollback 时把被删 delta 完整拷贝到 orphaned_branches（含 folded_state）；或明确移除这套机制。
- **损益**：收益是"回滚可恢复 + 消除误导性死代码"；代价中等（孤立分支保留需存储翻倍，或砍功能减维护面）。

### P1 谬误（按收益排序）

| # | 位置 | 问题 | 修复 | 损益 |
|---|---|---|---|---|
| P1-1 | stm-pipeline.js L298-330 | 超长 segment 非首位置时不拆分，整段超限提交 LLM | 第二个分支 push 前先对超长 seg 单独 splitIntraSegment | 纯收益 |
| P1-2 | stm-pipeline.js L512-517 | 事件按数组下标与 segment 一一映射，LLM 少输出即全部错位 | 按返回事件自身 msgRange 定位 | 纯收益 |
| P1-3 | adaptive-context.js L219 | `usableBase = Math.max(2000, ...)` 强抬下限，maxContext<2300 时压缩目标 > 模型上限，压缩完全失效 | `Math.max(1000, Math.min(2000, ...))` + goldenUpper 封顶 | 小模型恢复生效；需回归 golden tier |
| P1-4 | llm.js L585-651 | AbortError 无条件重试 ×2 轮 × 每轮 direct+proxy 双请求，全部等满 timeoutSec → 单次超时最坏 ~19s+ | AbortError 不重试；5xx/CORS 分开处理 | 超时路径 19s→3s；鲁棒性略降 |
| P1-5 | msg-id.js L29-33/L83-114 | 数字 id 退化格式（`send_date='0000-00-00...'`）在消息漂移后 fallback 断链，msg 引用永久丢失 | _legacyScan 尝试按 ID 尾段数字匹配 | 需 legacy 兼容测试 |
| P1-6 | schema.js L754-777 + state-versions L121-132 | `__proto__`/`constructor` path 可触发原型污染 / 校验绕过 | 赋值前过滤保留键 | 纯收益 |
| P1-7 | schema.js L444-475 + L366-407 | `required` 字段从不校验；`object` 类型零校验；`resolveSchemaPath` 不支持 `item_schema` → abilities 等子结构校验完全无效 | 补 required 检查 + item_schema 步进 | 数据质量提升；可能产生新告警噪音 |
| P1-8 | schema.js L762-770 | `__inc` 增量语法只对 `affection` 生效，其他 number 字段会把 `{__inc:{...}}` 对象写进 state | merge 时对任意 number 字段应用增量或 validate 只对 affection 产出 | 纯收益 |
| P1-9 | store.js L564-600 | rollbackByMsgIds：stm_index 残留死条目；LTM 移除后 stm_refs 的 parent_ltm/ltm_id 悬空 | 移除时同步清理索引与反向引用 | 一致性收益 |
| P1-10 | text-utils.js L53-59 | 普通对象当 Set，token `constructor`/`toString` 命中原型链 → 相似度虚高 | `Object.create(null)` 或 Map | 纯收益 |
| P1-11 | chat-telemetry.js L186 | `total_smartpush_injections = total_turns` 占位伪统计，UI 展示误导 | 真实计数或删除字段 | 纯收益 |
| P1-12 | garbage-collector.js L161-203 | listAllChatIds 漏 `memory_vaults` store → 孤儿 memory 数据永不清理 | 补 store 名 | 纯收益 |
| P1-13 | time-utils.js L3 | 字符串时间戳 → `NaN` 一路比较 false → 输出"NaN 个月前" | 入口 Number()/Date() 防御 + isNaN 早退 | 纯收益 |
| P1-14 | state-pipeline.js L268-287 | 派系扫描英文大小写敏感（alias `order` 匹配不到 `Order`） | 统一 toLowerCase | 纯收益 |
| P1-15 | ambiguity.js L148 / L98,L122 | 模式3 `resolved` 存"实体名+后缀"语义不一致；`replace` 无 `/g` 只替换第一处引用 | 只存实体名；全局替换 | 纯收益 |
| P1-16 | consolidate.js L116 | `present_characters[0].scene` 死代码 fallback（present_characters 是字符串数组） | 删除 fallback | 纯收益 |
| P1-17 | stm-pipeline.js L339 | 强制 `status='closed'` 覆盖 LLM 的 partial | 保留 partial 语义 | 需确认 consolidate 处理路径 |
| P1-18 | injection.js L178-180 + retrieval.js | SmartPush 实体链链路死：`entityChains` 初始化为 `{}` 后从未赋值，mergePipelines Step2 预取 / 场景外链块 / compileRetrievalBudget 三个消费点全部空转；legacy recall 工具链路正常 | ✅ 已修复 | `formatSmartContext` 补 `await lookupEntityChains(content, entityNames)` 接线（实体链从事件指针 `present_characters` 实时构建，非独立存储，与"数据层只留指针"兼容）；injection.js 补 import | injection.js、retrieval.js、entity-chain.test.js | `npm run build:all` 成功；`npm test` 全绿（30 文件 + 3 ratchet）；新增实体链单测 12 条（聚合/排序/跨层/回退/兼容） |

---

## 三、大优化空间（性能/复杂度）

### 存储层

| # | 位置 | 现状 | 优化 | 收益 | 代价 |
|---|---|---|---|---|---|
| D1 | store.js L151-200 | `openDB()` 无连接缓存，每轮 6-8 次 indexeddb.open | 模块级缓存 DB Promise + versionchange 失效重置 | 热路径延迟显著下降 | 需处理 DB 删除/升级失效 |
| D2 | state-versions.js L679-720 | compact 不清理旧 delta + 每次写全量快照 → 万轮对话累积几十 MB | compact 后单事务删除 seq<base 的旧 delta | 存储有界（线性→常数） | 删除需幂等，被删 seq 不再可回滚（与现状一致） |
| D3 | schema.js L650 + state-pipeline L874 | merge 全量深拷贝 + 每轮 2 次全量 stringify 判变化（每轮 3 次序列化） | 用 `mergeResult.changes.length` + `structuredClone` | 每轮省 2 次全量序列化，拷贝提速 2-5x | structuredClone 不拷贝函数（state 纯数据无影响） |
| D4 | schema.js L587-640/L1084-1112 | 每字段变更重复 ensureCharacterTemplate；expandTemplateFields **循环内**读 localStorage | 按 (charName, schemeKey, locked) 缓存解析结果；loadFieldLibrary 提出循环 | 热路径 localStorage 读从"字段数×角色数"降为个位数 | 需缓存失效钩子（saveCardConfig/saveTemplateLibrary 时清） |
| D5 | schema.js L859-867 | buildStateInjectionTable 全量历史消息拼接 + 每角色 indexOf | 只扫最近 N 条消息或建子串 Set | O(chars×msgLen)→O(msgLen+chars) | 需确认"本轮提及"语义可接受窗口截断 |
| D6 | state-versions.js L320-513 | fold 每个 delta 一个独立只读事务（链长 100 时 100 次事务） | 单事务连续 get 或按 chat_id 一次 getAll 内存重放 | fold 延迟 ~N 倍下降 | 需处理内存重放引用拷贝 |
| D7 | store.js L624-634 | getEffectiveTemplates 每次读 localStorage 且浅拷贝 mutate 共享默认模板 | 按 lib.updatedAt 缓存 + 深拷贝 | 热路径省 localStorage 读 | saveTemplateLibrary 时失效 |
| D8 | garbage-collector.js L71-111 | N chat 串行 + 每 key 3 次 openDB | openDB 缓存 + Promise.all 限并发 8-16 | 全量扫描分钟级→秒级 | 控制 IndexedDB 事务风暴 |

### 检索与注入

| # | 位置 | 现状 | 优化 | 收益 | 代价 |
|---|---|---|---|---|---|
| R1 | retrieval-filter.js L331-344 | **每次查询**对所有 STM 重算 alias tokens（baseTokens 已缓存但 aliasTokens 未缓存） | alias tokens 存入 _cache.knownMap（alias 指纹变化时重算） | 每查询 O(n×alias) → O(新增量) | 需 aliasMap 指纹比对 |
| R2 | injection.js L183-186 | 每次生成对全部 STM 全量 sortStmByMsgOrder + computeVisibleWindow 逐消息 countTokens（gpt-tokenizer 全量 BPE） | 排序结果缓存（消息 id 集合指纹）；可见窗口增量计数 | 每生成省 2 次全量 sort + N 次全量编码 | 缓存失效需指纹 |
| R3 | embedding.js L126-135 | 余弦相似度每次重算两向量范数（query 范数重复 N 次） | 入库时归一化，比较时纯点积 | 3×dim → 1×dim | 需同步写入侧与检索方语义 |
| R4 | embedding.js L96-118 | 批次嵌入串行（200 条=5 次串行 RTT）；单条缺失 → 整批返回 null 丢弃 | 2-3 并发 + 逐条 try/catch | 延迟降 1/2-1/3；坏条不拖垮整批 | 需处理部分失败 |
| R5 | contradiction.js L62-91 | N 个 claim 串行 BM25 + 串行 LLM 判定（5 条最坏 25s+，且阻塞发送） | Promise.all 限并发 2-3 | Σ延迟 → max | 需确认 API 限流余量 |
| R6 | tools.js L98-101 | chain 分支把完整 entries 全字段塞给 LLM（数 KB-tens KB token） | 只带精简 `[{id, ts, event}]` | 每轮省数百-数千 token | LLM 细节需再调 access |
| R7 | retrieval-filter.js L351-376 | 池 ≤ minResults 早返回路径直接改原始 STM 对象（`r.__type = e._type`） | 用浅拷贝 | 避免 `__type`/`__id` 写入持久化 vault | 极小 |

### 管线与遥测

| # | 位置 | 现状 | 优化 | 收益 | 代价 |
|---|---|---|---|---|---|
| P1 | chat-telemetry.js L74-116 | recordChatToken 双写 + 每次全量 parse/rebuild/save/neSync（每轮 6 次全量 JSON 读写） | 合并双写为一次；内存缓存 + 节流落盘；aggregates 增量维护 | 每轮 6 次 → 1 次 | 崩溃丢 1s 内增量；跨 tab 以最后写入为准 |
| P2 | adaptive-context.js L71-85 | trimMemoryVaultByKB 每删一个 section 就对全量剩余 join + countTokens → O(n²) | 维护累计 token，只对移除 section 单独计数 | O(n²) → O(n + k·avg) | section 级 token 和与整体有近似误差 |
| P3 | adaptive-context.js L165-167 | memory_vault 压缩每轮对全部 chat 重算 token | 增量差值（old-new） | O(iter×chatSize) → O(chatSize+iter×vaultSize) | 需精确 diff |
| P4 | token-stats.js L17-30 | 每次 LLM 调用全量 read-modify-write + neSync | 内存缓存 + 节流落盘 | 每轮省 N 次全量读写 | 崩溃丢增量 |
| P5 | stm-pipeline.js L342-419 | 同一 segment 文本被 formatTurnsText 格式化 3 次 | 缓存一次格式化结果复用 | O(3×文本)→O(文本) | 小重构签名 |
| P6 | auto-restore.js L149-151 | 每次加载无条件全量 saveChat（版本一致也写） | 仅 dbVault.version > chatVersion 时回填 | 消除加载路径最大 I/O 浪费 | 需确认版本比较语义 |
| P7 | llm.js L13-71 | 单次 LLM 调用对 ne_settings 全量 JSON.parse 3-5 次（llm/embedding/telemetry 各有一份重复实现） | 模块级缓存解析 + 失效标记 | 每次调用省 3-5 次数 KB parse | 设置热更新失效 |

---

## 四、小优化空间（常数级/低风险，代表性列表）

- **死代码清理**：stm-pipeline 三个 prompt 构建器（~250 行）、bm25-grouper 无调用方、state-pipeline tools 分支、pipeline-shared `_stripStateFieldsForMemory` 的 indexOf 循环 → Set。
- **JSON 解析入口收敛**：ambiguity.js / contradiction.js / consolidate.js 手写贪婪 JSON 提取 → 统一 safeJsonParse。
- **ltm-pipeline.js L90/L145**：`allSTM.find` 线性查找 → 一次构建 `id→entry` Map（O(ids×N)→O(N+ids)）。
- **splitStmsIntoContiguousGroups 双实现**（ltm-pipeline L169 vs consolidate L424）：`absMsgStart` 兜底 0 vs 999999 不一致 → 统一为单实现。
- **ne_settings 重复 parse**（llm/embedding/telemetry）：抽共享缓存工具。
- **llm.js L180-186**：TOKEN_OP_MAP 每次调用在函数体内重建 → 模块级常量。
- **embedding.js L186/189**：batch 与 query 两次请求串行 → Promise.all 并行。
- **turn-segmenter.js L27-28**：写回入参消息对象 `_idx/_absIdx` 副作用 → turn 结构内部保存。
- **context-window.js L10/L14**：role 未知一律当 assistant；rounds≤0 时首对即返回 → 边界加固。
- **params.js L72-78**：`turnsPerEvent` 缺失时 NaN 传播 → 入口归一化 `|| 3`。
- **template-llm.js L86-92**：lazy init 未 await async（当前碰巧同步赋值正确，脆弱模式）→ 缓存 pending promise。
- **template-llm.js L322-346**：PC 角色 exact/AI 双路径判定不一致（exact→'npc' vs AI→'pc'）→ 统一短路 _isPC。
- **template-defs.js L20**：DEFAULT_NPC_TEMPLATE 描述 "15 preset fields" 实际 13 → 改描述。
- **store.js L529-544**：appendSTMEntries 4 次遍历合并为 2 次；原地 sort 改 `slice().sort()`。
- **consolidate.js**：getNextEligibleStmId/getEligibleStmIds 重复 filter+sort 逻辑 → 抽取公共。
- **state-pipeline.js L51-64/L357**：字段集合每次调用重建 → 模块级惰性缓存。
- **ltm-pipeline.js L30**：`computeClosureSignals(openLtm, [])` 传空数组，sceneChange/entityOverlap 信号恒无效 → 传真实新 STM。

---

## 五、优化损益权衡总表

### A. 纯净收益（无风险，直接做）

| 项 | 说明 |
|---|---|
| P0-1 msgId 修正、P0-2 迁移挂起修复、P0-4 校验基准 | 确定性 bug 修复，零副作用 |
| 死代码删除 / 描述修正 / 伪统计修正 | 减维护面 |
| 缓存化类（openDB 连接、ne_settings 解析、字段集合、alias tokens、TOKEN_OP_MAP） | 有缓存失效问题需一并处理（见 B） |
| 大小写归一、Set 化、Map 化、单事务化 | 无行为变化 |

### B. 有小代价（收益明确，需配套治理）

| 项 | 代价 |
|---|---|
| 模板解析/字段库缓存（D4、D7） | 必须补 saveCardConfig/saveTemplateLibrary 失效钩子，否则热更新不生效 |
| embedding 归一化（R3） | 写入侧返回向量语义变化，检索方需同步；需一次回归 |
| 批量嵌入并发（R4） | API 限流余量需确认；部分失败需重试策略 |
| contradiction 并行化（R5） | 并发 LLM 调用触发限流风险 |
| adaptive-context 增量计数（P2/P3） | section/差值 token 与整体计数存在近似误差，需对账测试 |
| compact 清理旧 delta（D2） | 幂等设计；被删 seq 不可回滚（与现状一致，无行为回退） |
| 队列 rethrow 取消（P0-3） | 调用方错误暴露方式改变，需扫全部 enqueue 调用点 |
| msg-id 漂移兜底（P1-5） | legacy 兼容测试必须覆盖 |
| 孤立分支修复（P0-5） | 存储翻倍或砍功能，二选一需产品决策 |
| usableBase 下限修正（P1-3） | golden tier 回归测试 |

### C. 需产品决策（非纯技术问题）

| 项 | 决策点 |
|---|---|
| state_table 是否参与压缩（adaptive-context L209/L256-259） | 若 state 表是超预算主因，当前压缩永远碰不到它 |
| expandLayers 的 memory_vault 分支是 no-op（L192-195） | 只改层计数不恢复内容，调试快照误导 |
| segmentTurns 三级切分是否重建 | 当前退化为单区间，架构文档与实现不符 |

---

## 六、建议优先级路线图

1. **第一批（纯收益，1 次发版内）**：P0-1 msgId、P0-2 迁移挂起、P0-4 校验基准、P1-13 time-utils、P1-10 text-utils Set、P1-11 伪统计、P1-16 死代码 fallback —— 全部一行级修复，零风险。
2. **第二批（性能大头）**：D1 openDB 缓存（全项目最大热路径收益）→ P1/D3 双 stringify → P1/D4 模板解析缓存 → R1 alias tokens 缓存 → P1-1/P1-2 切分与事件映射修正。
3. **第三批（存储与可靠性）**：D2 compact 清理 → P0-3 队列毒化 → P0-5 孤立分支决策 → D6 fold 单事务。
4. **第四批（延迟与鲁棒性）**：P1-4 重试策略 → R3/R4 embedding → R5 矛盾检测并行 → P1 chat-telemetry 节流 → P2/P3 adaptive-context 增量计数。
5. **产品决策项**：C 组四项需用户定夺后再动。

---

## 八、UI 层与浏览器适配专项审计（第二轮，2026-08-01 追加）

> 触发背景：用户高频反馈"使用中卡顿 + 小型 UI 交互问题"，且浏览器适配问题体现于 UI 层。本轮覆盖 12 个 panel 文件 + style.css + index/bootstrap/ne-system-msg/chat-completion-patch。
> 编号：UI-x（交互谬误）、UIP-x（渲染性能）、UIB-x（浏览器适配）、UIS-x（监听器泄漏/小优化）。修复状态见"〇"表。

### 8.1 UI 交互谬误（确定性 bug / 静默失效）

| 编号 | 位置 | 问题 | 修复 | 损益 |
|---|---|---|---|---|
| UI-1 | panel-settings.js L723 | `setRetrievalEnabled` **从未 import**，每次保存设置必然 ReferenceError → channels 模式所有 API 通道配置/neSyncAll 全部不落盘 | L7 补 import | 纯收益（连坐 3 个 bug） |
| UI-2 | panel-settings.js ~L636 | embedding 通道 key 取 `nes_embedding_api_key`（真实 ID 是 `nes_embedding_key`）→ fetch models 永远无鉴权 | keyId 特判同 URL | 纯收益 |
| UI-3 | panel-usage.js L210/217/241 | 图表默认对象用 `sp`、消费方读 `.state` → 无数据日 State 序列断档 + state-only 月份误判"无数据" | 统一 `state:0` | 纯收益 |
| UI-4 | panel-state-cards.js L1208-1212 + panel-templates.js L1443-1447/L1491-1494 | `querySelectorAll('.xxx-item span')` 同时匹配字段名 + 类型两个 span → 保存模板/方案时把 `'string'/'number'` 混入字段引用并写入字段库（三处同源） | 改 `> span:first-child` 或加 data-name | 纯收益 |
| UI-5 | panel-state-cards.js L1816 | LTM 主行 title/event **未 HTML 转义**直接拼 innerHTML（STM 行已转义） | 补 escapeHtml | 正确性+安全 |
| UI-6 | panel-state-cards.js L565/L578-583 | writeState 无 catch 静默失败；保存后清理逻辑是死代码（元素从不创建），完全依赖全量重绘兜底；deleteCharacterCard L1509 同 | 加 catch/toast + 显式 exitCardEditMode | 消除"保存无反应" |
| UI-7 | panel-content.js L45 | 防刷新保护 class 列表漏 `.ne-inline-row`/`.ne-char-edit` → 用户在 pipeline 运行时手改 STM/LTM 行或卡片，下个 vault:updated 直接抹掉编辑内容（**数据丢失**） | guard 补两类编辑态 class | 一行修复 |
| UI-8 | ne-system-msg.js L129-156 | sendNeInteraction 在 ST 下降级为"`[文本]`假按钮"纯文本 → 用户点不动、onConfirm 永不触发（受害者：初始化提示的"查看模板库"按钮） | 真浮层交互或诚实降级纯通知 | 消除误导 |
| UI-9 | index.js L716-718 | `DOMContentLoaded` + `readyState==='interactive'` 双触发 bootNE 无守卫 → 工具**重复注册**、vault 双写 | `window.__ne_booted` 守卫 | 一行修复 |
| UI-10 | index.js L239-242 | 先 `await _bootstrapVault` 再绑事件 → 初始化期间首轮消息漏记 | 先 setupEventListeners 再 bootstrap | 消除竞态 |
| UI-11 | panel-settings.js ~L305 | `document.querySelectorAll('.ne-manual-controls')` 不感知 shadow DOM（扩展模式面板在 shadow root 内）→ 自适应上下文开关静默失效 | 改 panelQSA | 纯收益 |
| UI-12 | events.js L844 vs L920 | FIND_PROMPT `\\\\|` 与 CHAR_FIND `\\|` 转义层级不一致 → NE-BANNER 块可能永不被剥离，白白耗 token | 对齐转义 + 匹配冒烟测试 | 需按 ST regex 约定核实 |
| UI-13 | panel-settings.js L685-688 | stmBatch='auto' 字符串写入滑杆 value（非法）→ 滑杆位置与显示失真 | 关 auto 时回填上次有效数值 | 纯收益 |
| UI-14 | panel-settings.js L262 | temperature=0 被 `\|\|` 吞掉，0 值无法显示 | `typeof === 'number'` 判断 | 纯收益 |
| UI-15 | panel-settings.js L679-680 | `nes_enable_retrieval` 复选框从未被渲染 → 检索开关在设置页不可控（死路径） | 补 UI 或删死代码 | 产品决策 |
| UI-16 | panel-version-history.js L212-219/L453-463 | `_origVault` 单份缓存跨类型回滚串扰（先回滚 state 再回滚 memory 会撤销前者） | 按 type 分离缓存 | 纯收益 |
| UI-17 | panel-version-history.js L84/97 vs L420-421 | 面板硬编码加载 200 条 vs 配置 limit 默认 100/可配 500，两套口径不一致 | 统一 getLimit | 纯收益 |
| UI-18 | panel-state-cards.js L544/L539 | JSON 字段解析失败静默跳过；数值输入 "abc" → NaN 直接入库 | catch 提示 + Number.isFinite 校验 | 防数据污染 |
| UI-19 | panel-state-cards.js L1816/toggleInlineEdit | LTM 5 列行内联编辑列语义错位（把 STM ID 列当 scene 编辑） | 按 entryType 区分列映射 | 交互修正 |

### 8.2 渲染性能（"卡顿"第一来源，比 core 层热点更直接）

| 编号 | 位置 | 问题 | 修复 |
|---|---|---|---|
| UIP-1 | panel-content.js updateVaultViewerPopout（全文件） | 每次 `vault:updated`（300ms 防抖）**全量重建**所有角色/阵营/任务卡 + STM/LTM 全部 tbody + quick index + 重绑所有按钮 + setTimeout(50) 二次绑定；热路径 console.log 未剥离 | 区块级脏检查 / rAF 合并 / 事件委托；长列表分页（前 50 条+展开全部） |
| UIP-2 | panel-templates.js | 几乎每个操作（sort/tag/添加/删除/复制）全量 innerHTML 重建 + 全量重绑十几组事件 + 每卡同步读 localStorage | 事件容器级委托一次绑定；只重渲染卡片网格区 |
| UIP-3 | panel-settings.js 滑杆 oninput | 每次拖动 tick 全量 JSON.stringify settings + setItem + neSyncAll 全量扫描 → 设置页拖动卡顿 | 改 onchange / 300ms 防抖 |
| UIP-4 | panel-settings.js L220-334 + panel-popout.js L27 | 每次打开记忆面板重写整个设置面板 + 自动发两次 API 连接测试网络请求；未保存输入被清空 | 首次渲染 + 按字段 patch；删 popout 中调用 |
| UIP-5 | panel-version-history.js L106-185 | 时间线最多 200 条全量渲染（每条含 5 条明细） | 默认 50 条 + 加载更多 |
| UIP-6 | events.js L32-44 | 每条消息事件 `_neCheckChatIntegrity` 全量 O(n) 遍历 ×2 + pendingMessages 每次重算 token | 抽样检查 / token 统计移到 flush |

### 8.3 浏览器适配（本轮重点）

| 编号 | 位置 | 问题 | 修复 |
|---|---|---|---|
| UIB-1 | style.css（全文件 548 行） | **整体是死代码**：manifest `"css": ""` 为空、rollup 无 CSS、dist 未含 → 面板记忆表格无边框/按钮/文本域基础样式全部缺失，是"UI 变丑"反馈的主来源 | 合并仍被使用的规则进 injectBottomDrawerCSS，或恢复 manifest css + 发布 CDN |
| UIB-2 | panel-init.js L504-525 + panel-shared.js L375+ | Shadow DOM 模式下 `.ne-mobile` 响应式规则挂在 host 上、样式在 shadow 内 → **移动端适配全部失效**（减 padding/降字号/关模糊/模板单列布局均不生效） | `:host(.ne-mobile)` 前缀，非 shadow 分支保留双套选择器 |
| UIB-3 | events.js L805 | `pre:has(...)` 需 Chrome 105+/FF 121+，旧内核整条丢弃 | 拆成直接规则 `code.language-banner` |
| UIB-4 | 全站 z-index | 面板 100000/99999 盖过 ST 自带对话框（角色编辑/保存确认）；`inset` 定位 Chrome<87 失效 | 降层级到 ST 浮层区间并实测 |
| UIB-5 | style.css L128/L457 + panel-shared L175 | 硬编码 `#1e1e1e`/`#1a1a1a` textarea 与注入 CSS 强制 `#fff` 输入框并存，浅色主题观感不一致 | 统一 ST 主题变量 |
| UIB-6 | style.css L170 + events.js L806 | th nowrap + td 无 word-break → 长英文/URL 撑破面板；banner 固定 12-14px 不跟随 --mainFontSize | word-break:break-word；banner 用 em |

### 8.4 监听器泄漏（累积性小优化，一次修齐）

- **UIS-1** panel-shared.js showConfirm（~L816）：Esc keydown 只在按 Esc 时自移除，点确定/遮罩关闭则永久残留，每弹窗一个
- **UIS-2** panel-shared.js showHelpCard（~L869）：外部点击监听在 X 关闭时不移除
- **UIS-3** panel-shared.js closeVaultOverlay（~L681）：transitionend 监听重复关闭时永留（display:none 不触发过渡）
- **UIS-4** bootstrap.js L80-98：全局 keydown 无去重守卫，init 双跑时累加
- **UIS-5** panel-init.js L505-525：ResizeObserver 存 overlay 但 close 时从不 disconnect
- **UIS-6** panel-version-history.js L366-386：slider oninput 每事件写 localStorage + neSync（与 UIP-3 同源，改 onchange 一并解决）

### 8.5 UI 层优先级建议

1. **第一批（一行级止血，零风险）**：UI-1（import 补漏）、UI-9（boot 守卫）、UI-7（编辑保护补 class）、UI-2/UI-3（key 修正）、UI-4 三处（选择器修正）
2. **第二批（浏览器适配）**：UIB-1（style.css 复活/合并）、UIB-2（shadow 移动端）
3. **第三批（卡顿根治）**：UIP-1 增量渲染 + UIP-2 事件委托 + UIP-3 保存防抖
4. **随发版清理**：UI-5 转义、UI-6 静默失败、UI-8 假按钮、UIS-1~6 监听器泄漏

---

## 附：审计方法论说明

- 所有"死代码"结论均经 `grep` 全 src 目录确认无调用方（buildCursorPrompt 等 4 个 prompt 构建器、preGroupItems、orphaned_branches put 路径、tools 分支）。
- 所有"必然触发"谬误均做了最小场景推演（如 msgRange 越界用"全局 idx 42-45 vs 本次 4 条"验证）。
- 复杂度结论基于实际数据规模：单 chat STM 上千条、state 数十角色×15+ 字段、版本链 ≤500 活跃。
