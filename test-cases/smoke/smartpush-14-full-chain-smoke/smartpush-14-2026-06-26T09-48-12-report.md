# 全链路冒烟测试（STM + LTM + SmartPush + State + 注入） — 测试报告
运行时间: 2026-06-26T09:48:12.391Z
实际轮次: 8
总耗时: 11 分 24 秒

## 断言结果

### 结构性断言
- [x] `exists`: exists: smartpush_injection 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_injection >=50 → **PASS**
- [x] `not_contains`: not_contains: smartpush_injection 不含 "→stm:" → **PASS**
- [x] `exists`: exists: smartpush_prompt 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_prompt >=200 → **PASS**
- [x] `exists`: exists: stm_events 不存在 → **PASS**
- [x] `min_length`: min_length: pipeline_responses >=50 → **PASS**
- [x] `not_contains`: not_contains: pipeline_responses 不含 ""error"" → **PASS**
- [x] `contains`: contains: pipeline_responses 含 "\"ltm_decision\"" → **PASS**
- [x] `exists`: exists: ltm_state 不存在 → **PASS**
- [x] `exists`: exists: pipeline_changes 不存在 → **PASS**
- [x] `min_length`: min_length: pipeline_changes >=1 → **PASS**
- [x] `not_contains`: not_contains: pipeline_changes 不含 "error" → **PASS**
- [x] `exists`: exists: state_block_instruction 不存在 → **PASS**
- [x] `min_length`: min_length: state_block_instruction >=20 → **PASS**

### 语义性断言
- [x] SmartPush 注入是否包含与对话内容相关的具体记忆信息（而非空话/占位符）？ → **PASS**
  - 评估: 注入内容包含具体记忆信息（两人相识场景、阳台调侃、新书对赌约定等），非空话或占位符。
- [x] 注入内容是否以自然语言叙事呈现（而非 JSON dump 或碎片化 stm_xxx 列表）？ → **PASS**
  - 评估: 注入以自然语言叙事呈现（如「江岚码完开头，找林晚星试读，同时趁安然没起改第二版」），未出现 JSON dump 或碎片化 stm_xxx 列表。
- [ ] STM 提取事件是否覆盖了该轮对话中的重要情节？ → **FAIL**
  - 评估: 本轮对话包含多个重要情节（江岚早起写稿、安然起床两人互动、找林晚星试读），但提取的 STM 事件仅覆盖「江岚码完开头，找林晚星试读」，遗漏了安然起床与两人互动的关键情节。
- [x] STM+LTM 合流管线是否正常工作（STM 提取同时输出了 ltm_decision，包含 action + updated_title）？ → **PASS**
  - 评估: STM+LTM 合流管线正常工作：STM 提取时同时输出了 ltm_decision（含 action 和 updated_title），决策为 append，标题与事件描述匹配。
- [x] State 管线是否正常执行，state_changes 是否有实际的字段路径（characters.* 而非空对象）？检查 pipeline_changes 是否至少有一轮包含非空变更。 → **PASS**
  - 评估: State 管线正常执行，state_changes 有实际字段路径（characters.安然，affection 从 10 变更为 8，current_mood 和 inner_thoughts 均有更新），非空对象。
- [x] ne_state_table 和 ne_char_block 注入指令是否非空且包含正确格式？检查 trace 中这两项注入的文本长度和关键标记。 → **PASS**
  - 评估: ne_state_table 和 ne_char_block 注入指令非空且格式正确：在最近几轮对话的回复中均出现正确格式的<!--NE-BANNER-->状态栏和<!--NE-CHAR-->块，内容无缺失。
- [x] trace 中所有 pipeline LLM 调用的 response 是否都成功返回了有效 JSON（无截断、无 parse error）？ → **PASS**
  - 评估: trace 中所有 pipeline LLM 调用的 response 均成功返回有效 JSON（如 ltm_decision JSON 结构完整，state_changes 格式正确），无截断或 parse error。
- [ ] trace 中是否出现过 pipeline LLM 调用 fallback（secondary API → TH）？如有，是否仍正常工作？ → **FAIL**
  - 评估: trace 中未出现 pipeline LLM 调用 fallback 的迹象（所有 LLM 调用看起来都是直接返回正常 JSON，无 secondary API 触发痕迹）。无法判断 fallback 机制是否正常工作，因为主路径未失败。 (超时截断，按不通过处理)

## 总结
**未通过。** 存在失败的断言，详见上方。

**结束类型**: forced_max_rounds
