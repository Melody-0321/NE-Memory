# 全链路冒烟测试（STM + LTM + SmartPush + 注入） — 测试报告
运行时间: 2026-06-18T12:43:48.730Z
实际轮次: 8
总耗时: 6 分 58 秒

## 断言结果

### 结构性断言
- [x] `exists`: exists: smartpush_injection 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_injection >=50 → **PASS**
- [x] `not_contains`: not_contains: smartpush_injection 不含 "→stm:" → **PASS**
- [ ] `exists`: exists: smartpush_prompt 不存在 → **FAIL**
  - 详情: 不存在, 期望=true
- [ ] `min_length`: min_length: smartpush_prompt >=200 → **FAIL**
  - 详情: 实际长度=0 (要求>=200)
- [ ] `exists`: exists: stm_events 不存在 → **FAIL**
  - 详情: 不存在, 期望=true
- [ ] `min_length`: min_length: pipeline_responses >=50 → **FAIL**
  - 详情: 实际长度=0 (要求>=50)
- [x] `not_contains`: not_contains: pipeline_responses 不含 "\"error\"" → **PASS**
- [ ] `contains`: contains: pipeline_responses 含 "\\\"ltm_decision\\\"" → **FAIL**
  - 详情: 缺少: \"ltm_decision\"
- [ ] `exists`: exists: ltm_state 不存在 → **FAIL**
  - 详情: 不存在, 期望=true

### 语义性断言
- [ ] SmartPush 注入是否包含与对话内容相关的具体记忆信息（而非空话/占位符）？ → **FAIL**
  - 评估: 本轮注入内容仅为系统状态维护标记（[ℹ Current state is maintained in the World Book: NE_Memory_State]），未包含任何与对话内容相关的具体记忆信息，属于占位符性质。
- [ ] 注入内容是否以自然语言叙事呈现（而非 JSON dump 或碎片化 stm_xxx 列表）？ → **FAIL**
  - 评估: 注入内容为方括号包裹的系统提示格式，非自然语言叙事呈现。
- [ ] STM 提取事件是否覆盖了该轮对话中的重要情节？ → **FAIL**
  - 评估: 本轮注入内容本身是管道输入而非 STM 提取输出，无法从该注入内容判断 STM 是否覆盖了对话中的重要情节。 (超时截断，按不通过处理)
- [ ] STM+LTM 合流管线是否正常工作（STM 提取同时输出了 ltm_decision，包含 action + updated_title）？ → **FAIL**
  - 评估: 注入内容为管道输入而非输出，无法从中观测 STM+LTM 合流管线的执行状态（ltm_decision 等）。 (超时截断，按不通过处理)
- [ ] trace 中所有 pipeline LLM 调用的 response 是否都成功返回了有效 JSON（无截断、无 parse error）？ → **FAIL**
  - 评估: 注入内容为管道输入而非 trace 中的 LLM 调用的 response，无法据此判断 pipeline LLM 是否成功返回有效 JSON。 (超时截断，按不通过处理)
- [ ] trace 中是否出现过 pipeline LLM 调用 fallback（secondary API → TH）？如有，是否仍正常工作？ → **FAIL**
  - 评估: 注入内容不包含 pipeline LLM 调用或 fallback 信息，无法判断相关情况。 (超时截断，按不通过处理)

## 总结
**未通过。** 存在失败的断言，详见上方。

**结束类型**: forced_max_rounds
