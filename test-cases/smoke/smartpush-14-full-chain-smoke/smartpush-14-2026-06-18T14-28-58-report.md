# 全链路冒烟测试（STM + LTM + SmartPush + 注入） — 测试报告
运行时间: 2026-06-18T14:28:58.198Z
实际轮次: 6
总耗时: 5 分 37 秒

## 断言结果

### 结构性断言
- [x] `exists`: exists: smartpush_injection 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_injection >=50 → **PASS**
- [x] `not_contains`: not_contains: smartpush_injection 不含 "→stm:" → **PASS**
- [ ] `exists`: exists: smartpush_prompt 不存在 → **FAIL**
  - 详情: 不存在, 期望=true
- [ ] `min_length`: min_length: smartpush_prompt >=200 → **FAIL**
  - 详情: 实际长度=0 (要求>=200)
- [x] `exists`: exists: stm_events 不存在 → **PASS**
- [x] `min_length`: min_length: pipeline_responses >=50 → **PASS**
- [x] `not_contains`: not_contains: pipeline_responses 不含 "\"error\"" → **PASS**
- [ ] `contains`: contains: pipeline_responses 含 "\\\"ltm_decision\\\"" → **FAIL**
  - 详情: 缺少: \"ltm_decision\"
- [x] `exists`: exists: ltm_state 不存在 → **PASS**

### 语义性断言
- [x] SmartPush 注入是否包含与对话内容相关的具体记忆信息（而非空话/占位符）？ → **PASS**
  - 评估: 注入包含辛耶表达庆幸、赛奥提议存档、蕾娜承诺记住个人细节等具体事件，信息充实，并非空话或占位符。
- [x] 注入内容是否以自然语言叙事呈现（而非 JSON dump 或碎片化 stm_xxx 列表）？ → **PASS**
  - 评估: 事件以自然语言叙述呈现，虽有结构化标题，但仍是完整叙事，无 JSON dump 或碎片化列表。
- [x] STM 提取事件是否覆盖了该轮对话中的重要情节？ → **PASS**
  - 评估: 事件覆盖了前几轮对话的关键情节：辛耶的长独白、赛奥的存档提议、蕾娜的承诺等，无重大遗漏。
- [ ] STM+LTM 合流管线是否正常工作（STM 提取同时输出了 ltm_decision，包含 action + updated_title）？ → **FAIL**
  - 评估: 仅看到注入的记忆内容，无法观测到 STM 提取时的 ltm_decision 输出及内部管线状态。
- [ ] trace 中所有 pipeline LLM 调用的 response 是否都成功返回了有效 JSON（无截断、无 parse error）？ → **FAIL**
  - 评估: 缺少 trace 日志，无法验证所有 pipeline LLM 调用的 response 是否均为有效 JSON。
- [ ] trace 中是否出现过 pipeline LLM 调用 fallback（secondary API → TH）？如有，是否仍正常工作？ → **FAIL**
  - 评估: 缺少 trace 信息，无法判断是否发生了 fallback 调用及其结果。

## 总结
**未通过。** 存在失败的断言，详见上方。

**结束类型**: semantic_fail
