# 全链路冒烟测试（STM + LTM + SmartPush + 注入） — 测试报告
运行时间: 2026-06-24T04:57:57.879Z
实际轮次: 8
总耗时: 7 分 23 秒

## 断言结果

### 结构性断言
- [x] `exists`: exists: smartpush_injection 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_injection >=50 → **PASS**
- [x] `not_contains`: not_contains: smartpush_injection 不含 "→stm:" → **PASS**
- [x] `exists`: exists: smartpush_prompt 不存在 → **PASS**
- [x] `min_length`: min_length: smartpush_prompt >=200 → **PASS**
- [x] `exists`: exists: stm_events 不存在 → **PASS**
- [x] `min_length`: min_length: pipeline_responses >=50 → **PASS**
- [x] `not_contains`: not_contains: pipeline_responses 不含 ""error"" → **PASS**
- [x] `contains`: contains: pipeline_responses 含 "\"ltm_decision\"" → **PASS**
- [x] `exists`: exists: ltm_state 不存在 → **PASS**

### 语义性断言
- [x] SmartPush 注入是否包含与对话内容相关的具体记忆信息（而非空话/占位符）？ → **PASS**
  - 评估: 注入内容包含具体的记忆信息：阳台互动细节（承认硬了、展示腹肌）、浴室门口的毛巾指引、火锅底料归属和类型纠错等，均为有实质情节的记忆，非空话或占位符。
- [x] 注入内容是否以自然语言叙事呈现（而非 JSON dump 或碎片化 stm_xxx 列表）？ → **PASS**
  - 评估: 注入内容以自然语言叙事呈现，按章节组织，包含时间、场景、事件描述和角色知情度标记。虽有KB标记，但整体为可读叙事，非JSON dump或碎片化stm列表。
- [x] STM 提取事件是否覆盖了该轮对话中的重要情节？ → **PASS**
  - 评估: STM提取事件覆盖了该轮对话中的重要情节：阳台身体试探（21:35-21:38）、浴室门口毛巾与火锅提议（21:42）、底料归属纠错与洗菜分工辩论（21:43-21:44），均为对话核心事件。
- [x] STM+LTM 合流管线是否正常工作（STM 提取同时输出了 ltm_decision，包含 action + updated_title）？ → **PASS**
  - 评估: 管线调用记录中，STM提取（state_changes+events）与LTM决策（ltm_decision）同时输出。LTM决策包含action（append/close_and_new）和updated_title+updated_event，合流管线正常工作。
- [x] trace 中所有 pipeline LLM 调用的 response 是否都成功返回了有效 JSON（无截断、无 parse error）？ → **PASS**
  - 评估: trace中各pipeline LLM调用返回了有效JSON结构：state_changes、events、ltm_decision等字段均完整闭合，未出现截断或parse error。
- [x] trace 中是否出现过 pipeline LLM 调用 fallback（secondary API → TH）？如有，是否仍正常工作？ → **PASS**
  - 评估: 提供的trace片段中未出现fallback（secondary API → TH）的迹象。所有管线调用均正常完成，无降级处理记录。

## 总结
**通过。** 所有断言通过。

**结束类型**: forced_max_rounds
