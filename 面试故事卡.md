# 面试故事卡 — NE Memory Engine 项目

> 用法：面试前通读一遍，不需要背诵。每个故事记住"情境→问题→行动→结果+反思"这条线即可。
> 面试官问到时，按这条线自然展开，不要机械复述。
>
> 项目概况：v6.8.0，~17,500 行代码，49 个源模块，7 个 IndexedDB ObjectStore，26 单元测试 + 23 LLM 集成测试，101 篇设计文档从需求到复盘全链可追溯。

---

## 故事一：检索系统选型 —— 用实验数据做决策

### 面试官可能会问的形式

- "你的混合检索是怎么设计的？为什么选这个方案？"
- "你简历里提到 BM25+embedding fusion，能展开讲讲吗？"
- "你提到自建评估基准，具体怎么做的？"

### 回答框架

**情境**：NE Memory Engine 需要解决 AIRP 场景下的长对话记忆召回问题。用户对话几万条消息之后，怎么精准找回跟当前对话相关的历史记忆？这不是一个"装个向量数据库就搞定"的问题。

**问题**：纯 BM25 关键词匹配太机械——用户说"我今天心情不好"，BM25 可能找回三个月前一次提到"心情"的对话，但实际上那段记忆跟当前完全无关。纯向量检索语义泛化太强——有时会召回看起来相关、但实际上不准确的内容。融合策略怎么设定权重？注入结构怎么排序？这些都不能靠直觉。

**行动**：我基于真实 AIRP 对话场景自建了一个测试数据集，定义了几个评估指标——主要是 recall@N 和 precision。然后在同一个测试集上跑了多组对比实验：

- 纯 BM25 vs 纯向量 vs 各种融合权重组合
- 权重是非对称的——BM25 和 embedding 分不能简单 1:1 混合，因为它们的分数分布差异很大
- 对比了 4 种注入结构：纯相关性排序、纯时间排序、分角色时间排序、分角色时间排序+相关性融合

**结果**：测试数据跑出来，RRF fusion（Reciprocal Rank Fusion）在 alpha=0.30（BM25 权重 30%）下效果最好——BM25 的关键词匹配能力和 embedding 的语义泛化各取所长。注入结构上，分角色时间排序+相关性融合的召回率显著优于纯相关性——因为 AIRP 场景下，同一角色的历史记忆之间有天然的时间连续性，不能只看单条记忆的相关性。

**反思**：这个过程的教训是——做 RAG 不能只看论文里通用的 best practice。每个场景下最优方案是不一样的，必须用你自己的场景数据去验证。我如果不是自己建了测试集去跑对比，靠直觉选权重，效果会差很多。这个习惯也延续到了整个项目——我可以给你看，目前项目有 22 个确定性单元测试和 23 个 LLM 集成测试用例，覆盖了从智能检索、State 管线到冒烟测试的完整链路。

### 面试官可能会追问的

**Q："你的测试集是怎么构建的？"**

A：从真实用户对话中提取了约两百组 query-memory 对。每个 query 对应一组应该被召回的黄金标准记忆。构建方式是一半人工标注、一半从已有对话中反向提取——如果某条后续消息明显引用了之前的记忆，那我就把那段记忆标注为"应该被召回"。

**Q："评估指标怎么选的？"**

A：主要看 recall@10——因为记忆注入有上下文窗口限制，每次最多注入 10 条左右。precision 在记忆场景下相对没那么关键——多召回一条弱相关记忆的代价，远低于漏掉一条关键记忆。

---

## 故事二：存储层拆分 —— v7 解决并发写覆盖

### 面试官可能会问的形式

- "你的架构是怎么演进的？"
- "你在架构上做过的最大调整是什么？"
- "你是怎么处理并发写入的？"

### 回答框架

**情境**：NE Memory Engine 有两条核心管线——State 管线负责每轮对话更新角色状态字段，STM 管线负责批量提取短时记忆条目。经过早期重构，两条管线已经从串行改成了独立的 Promise 队列——队列内串行、队列间并行。

**问题**：并行后出现了一个严重问题。两条管线都通过 `read-vault → modify-in-memory → write-vault` 的模式操作同一个 IndexedDB ObjectStore。因为它们跑在不同的异步队列里，没有任何事务保护，后写入的管线直接用整条记录覆盖了先写入的内容。用户反馈"角色状态更新偶尔丢失""短期记忆明明提取了但读不到"——这不是确定性 bug，是概率性的，取决于两条管线的 LLM 调用谁先返回。更难排查的是，`readVault` 是合并视图——读了 STATE 和 MEMORY 两个域的数据合并返回——导致 STM 管线在某些路径上把 State 字段带进了 Memory Store，造成**跨域数据污染**。

**行动**：v7.0 做了一次彻底的存储层拆分——把原本共用的 `vaults` ObjectStore 拆成 `state_vaults` 和 `memory_vaults` 两个独立 Store。总共 8 个 ObjectStore：state_vaults、memory_vaults、state_deltas（版本链增量）、memory_versions（版本链版本）、active_chains（活跃链头指针）、orphaned_branches（孤立分支）、card_configs（角色卡配置模板）、以及从 localStorage 迁移出来的模板库。

拆分不只是"建两个表"——还涉及：
- 设计 `readVault` 合并读取的字段归属规则（哪些字段读自 STATE_STORE，哪些读自 MEMORY_STORE）
- v7 迁移脚本：把旧 single-store 的 vault 按字段白名单自动拆分并逐条验证
- 双向恢复逻辑 `loadVault`：IndexedDB 丢失时从 SillyTavern 聊天文件的 `chat_metadata.ne_vault` 恢复，反过来 IndexedDB 更新时回写到聊天文件，版本号比较按状态/记忆两个维度独立判断
- 所有管线调用点从 `readVault`/`write` 切到 `readState`+`readMemory` / `writeState`+`writeMemory`，保证每个管线只读写自己域的数据

**结果**：拆分后并行写入竞态彻底消失——因为 State 和 Memory 不再共享同一个 ObjectStore。存储层的这次重构是 v7-v8 最核心的架构优化，后续版本的所有功能都建立在这个基础上。

**反思**：这件事教会我一条教训——并发安全的正确解法不是加锁，是隔离。IndexedDB 的 transaction 粒度是 ObjectStore 级别——拆 Store 比在应用层排队更可靠，也更简洁。如果不是拆 Store 而是继续在应用层加互斥控制，复杂度会指数级增长。

### 面试官可能会追问的

**Q："v7 迁移脚本怎么保证数据不丢？"**

A：迁移分成三步。第一步读取旧 `vaults` store 的单条记录，按 `STATE_CONTENT_FIELDS` 和 `MEMORY_CONTENT_FIELDS` 两个白名单拆分到两个独立对象。第二步并发写入 `state_vaults` 和 `memory_vaults`。第三步重新读取两个新 Store，逐字段验证每个 key 都存在且内容与原记录一致。如果验证失败，迁移脚本报错阻止后续操作。迁移完成后设 `localStorage.ne_v7_migrated` 标记防止重复触发。

**Q："拆 Store 和事务锁之间为什么选了拆 Store？"**

A：IndexedDB 的读写事务模型是每个 ObjectStore 级别的——不同 Store 的写操作天然互不阻塞。如果把所有数据放在一个 Store 里，即使你在应用层加互斥队列，队列本身只是串行化 Promise——但队列和队列之间（例如 State 管道和 STM 管道各自的独立队列）仍然可能交叉。拆 Store 利用的是 IndexedDB 自身的隔离保证，比应用层控制更可靠。

---

## 故事三：版本链审计 —— 发版前全量扫描的 11 个 Bug

### 面试官可能会问的形式

- "你怎么保证代码质量？"
- "你遇到过最复杂的一次修复是什么？"
- "你做过代码审计吗？"

### 回答框架

**情境**：v6.8.0 基本功能稳定后，我决定在发版前做一次全量一致性审计——不是为了修某个已知 bug，而是系统性地查找"重复造轮子导致的行为不一致"。

**问题**：项目运行了 46 个 commit 积累了 v6.8，代码量到了约 17,500 行。早期开发的快速迭代意味着不可避免地有一些实现上的不一致——两个函数做同一件事但行为有细微差异，字段列表在多处重复定义增加了遗漏风险，并发模型在某些边缘场景下有竞态。这些问题不会在正常使用中立刻暴露，但如果碰巧遇到，会造成数据损坏。

**行动**：我写了一个扫描计划，把审计维度分成四个方面——Pipeline 并发安全、版本链引擎、存储层读写、事件处理流程——每个维度启动一个搜索子代理并行扫描。最终发现 11 个严重程度不一的问题：

**严重的（4 个）：**
- `rebuildMemoryVault` 函数把 memory 数据（STM/LTM entries）写入了 `state_vaults` ObjectStore——函数名写的是 Memory，实际读写的是 STATE_STORE。原因是 `rebuildStateVault` 对称实现的复制错误
- `remove()` 函数中索引名 `chat_id_seq` 与 Store 定义中的 `chat_id` 不匹配，导致删除操作静默抛异常，聊天数据永久残留在 IndexedDB
- `loadVault` 双向恢复逻辑用单一 `version` 比较两个独立 Store 的数据，导致其中一个维度的最新更新被聊天文件旧缓存覆盖
- `consumeNeCharBlocks`（NE-CHAR 角色状态更新）没有进入 State 管线的串行队列，直接写 STATE_STORE，和正在运行的 State LLM 产生丢失写

**高危的（3 个）：** `_detectAndRollback` 的异常会永久毒化 STM 管线队列、`rollbackState/rollbackMemory` 不检查负数 targetSeq、STM 管线内的 `rollbackState` 没有排空 State 管线队列导致回退被覆盖

**中等（4 个）：** `saveStateVault/saveMemoryVault` 吞掉写入错误、`initializeStateChain` 的 fire-and-forget 导致初始快照未就绪、以及其他几处防御性守卫缺失

全部修复只用了约 40 行净增，11 个文件，构建和 26 个单元测试文件全部通过。

**结果**：审计修复之后，存储层、管线层、版本链引擎的逻辑完全收敛，不再有重复造轮子导致的行为不一致。最关键的三条修复是 Store 级别的（正确的 Store 写入、正确的索引名、独立的版本号比较）——如果带着这些 bug 发版，用户在某些路径下会造成静默数据损坏，而且很难回溯。

**反思**：这次审计让我意识到，"代码能跑"和"代码不会在边缘情况下损坏数据"之间有巨大的鸿沟。独立开发最大的风险就是没有 code review 帮你发现这些问题，所以我建立了一个审计流程——每次大版本发版前，并行扫描所有核心模块，按严重程度分级修复。我现在可以把这 4 个维度的审计框架直接带到团队里。

### 面试官可能会追问的

**Q："你一个人怎么做到这种系统扫描的？"**

A：我把审计分成 4 个独立维度，利用搜索子代理并行扫描——每个代理负责一个模块的深度分析，返回结构化的风险列表。我自己负责汇总和优先级排序。本质上这是把"另一个人 review 你的代码"变成了"多个代理从不同角度 review 你的代码"。

**Q："最大的教训是什么？"**

A：重复代码是 bug 的温床。这次审计里最严重的几个 bug——write 错 Store、索引名不匹配、版本号合并比较——根源都是同一段逻辑在两个地方独立实现但没有保持同步。如果项目一开始就用 TypeScript 的类型系统约束 Store 类型和字段归属，这几个 bug 在编译期就会被抓到。但我当时的场景（SillyTavern 的运行时脚本注入）只能用纯 JS，所以这些约束全靠人工维护。

---

## 故事四：Cascade Loop 排查 —— 从现象到根因的 5 步

### 面试官可能会问的形式

- "你遇到过最头疼的 bug 是什么？怎么解决的？"
- "你是遇到问题后怎么排查的？"

### 回答框架

**情境**：NE Memory Engine 运行在 SillyTavern 平台上，它需要调用 ST 的 API 来触发 LLM 生成。

**问题**：有一天用户反馈，消息处理出现了递归级联——发一条消息触发了一个回环，不断重复生成。

**排查过程**（走了 5 步才找到根因）：

- **第 1 步，诊断探针**：先加了一个 MutationObserver 监控 DOM 变化，确认是不是 UI 层触发的重复渲染。结果不是。
- **第 2 步，spurious trigger guard**：怀疑是 ST 的事件系统异常触发了多次 `onMessageReceived`，加了一个虚假触发守卫。还是没解决。
- **第 3 步，re-entrancy guard**：怀疑是自身代码重入——我的 handler 在处理事件时又触发了同一个事件。加了重入守卫。仍然没解决。
- **第 4 步，排查调用链**：开始怀疑问题不在我这边，而是在我和 ST 之间的边界。追溯了整个调用链——`generateRaw` / `generateQuietPrompt` 内部调用了 ST 的 `Generate()` → ST 的 `Generate()` 完成后触发了 `GENERATION_AFTER_COMMANDS` 事件 → 这个事件又被我的 pipeline 监听到 → 又调了一次 `generateRaw`。环形依赖，无限递归。
- **第 5 步，修复**：在 `GENERATION_AFTER_COMMANDS` 处理中加了事件来源标记——如果是我的代码触发的 `Generate()`，就不响应后续的 `GENERATION_AFTER_COMMANDS`。

**结果**：5 个 commit 终于修掉。但这个故事真正的价值不是修了一个 bug，而是之后我建立了一个模式——任何在事件回调中调用可能触发同事件的外部 API 的地方，都必须有重入守卫。复盘之后我把这个模式写进了规范，后面没有再出现同类问题。

**反思**：这个排查过程的问题是——前 3 个 commit 是"如果我在第一层就先检查调用链，可能 2 个 commit 就能解决"。但我当时对 ST 的事件系统不够熟悉，不知道 `Generate()` 会触发 `GENERATION_AFTER_COMMANDS`。教训是：和外部平台集成时，先搞清楚对方的事件模型和触发链，再假设自己的代码没问题。

### 面试官可能会追问的

**Q："你在集成第三方平台时，一般怎么避免这类问题？"**

A：两个原则——第一，集成前先画调用时序图，明确"我的代码调用平台 API → API 返回什么事件 → 这些事件会不会被我的 handler 再次捕获"。第二，任何外部调用都加超时保护和重入守卫，不管你觉得会不会触发级联。

**Q："如果你在团队里做，这个排查过程会更快吗？"**

A：会，因为团队里可能有更熟悉 ST 的人直接指出问题。但一个人做的优势是——你对整个调用链的理解会更深入，因为每个环节都得自己摸。事后我也确实因为这个排查过程，对整个 ST 的事件系统有了系统性的理解。

---

## 通用应对：如果面试官问"你一个人做 vs 团队做的区别"

可能被问到："你一直是独立开发，怎么证明你适合团队协作？"

可以这样答：

> 我目前确实是独立开发，但从 NE Memory Engine 的开发过程来看，我建立了一整套规范体系——35+ 篇设计文档可追溯、模块风险分级、条件激活的项目规则、commit message 严格遵循 conventional commits。这些规范本质上就是"团队协作所需的上下文共享机制"——在团队里，你不是靠口头传达来同步上下文，而是靠文档和规则文件。
>
> 具体来说，我会做全量代码审计（4 维度并行扫描、按严重度分级修复）、建测试体系（26 单元 + 23 LLM 集成 + ratchet 架构约束检查）、维护构建系统（dev/release 双模式、pre-commit hook 自动构建）。
>
> 我之所以想进入团队，正是因为我已经把"单人能做多大"推到了极限——接下来需要学习的是多人场景下的协作流程：code review、需求评审、跨职能沟通。我有独立交付能力，缺的是协作经验，而这是我求职的首要目标。
