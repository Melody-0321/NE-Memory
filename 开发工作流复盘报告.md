# NE-Memory 开发工作流复盘报告

> 数据源：xm 父项目（66 篇架构文档 + git log）+ ne-memory 子项目（500+ 条提交 + 35 篇实施文档 + 18 版 CHANGELOG）+ Trae 对话记忆系统（session_memory + topics + project_memory）
> 
> 时间跨度：2026-05-28 ~ 2026-07-15（共 49 天）
> 
> 生成日期：2026-07-15

---

## 一、完整工作流时间线

```
2026/5/28 ━━━━━━━━━━━ 6/9 ━━━━━━ 6/13 ━━━━ 6/25 ━━━━ 7/3 ━━━━ 7/8 ━━━━ 7/15
  │                    │         │         │         │         │         │
  │  阶段零：叙事引擎   │  阶段一 │ 阶段二  │ 阶段三  │ 阶段四  │ 阶段五  │
  │  P1-P3 骨架       │  0→1   │  深化   │  架构   │  UI/    │  Schema │
  │  架构设计           │  发布   │  Retrieval│ 重构   │ 质量   │  系统   │
  └────────────────────┴─────────┴─────────┴─────────┴─────────┴─────────┘
```

---

### 阶段零：叙事引擎 P1-P3 骨架 + 架构设计（5/28 ~ 6/5）

**仓库位置**：`d:\SillyTavern\xm`（父项目）
**轨迹载体**：66 篇 `.trae/documents/` 架构文档

| 日期 | 核心活动 | 关键产出 |
|------|----------|----------|
| 5/28 | 叙事引擎项目初始化 | `feat: 完成叙事引擎P1-P3全量基础骨架搭建` |
| 5/29 | 核心功能实现 + 前端配置页重构 | STM/LTM 提取管线、IndexedDB vault 存储层 |
| 5/30 | CDN 分发链路建立、TH 脚本装载器、抽屉式 UI | v0.2.0 纯前端架构首版 |
| 5/31 | State Schema 模块 + Tool 体系整合 | v0.3.0，字段级约束 + 工具注册框架 |
| 6/1 | Smart Push + 记忆检索服务上线 | v0.4，Process History、LLM 日志面板、Vault 导出/导入 |
| 6/2 | 事件注册健壮性、CDN 稳定化、副 API 本地代理 | 适配层 6 连修 + 双格式 LLM 路由 |
| 6/3 | 流水线守卫重构、并发控制、阈值修复 | `pipeline-guard-state-machine.md`（5 态管线模型） |
| 6/4 | 动态 State 字段发现、World Book 整合、Power Slots 初始化 | `ne-memory-core` Tier 1 & 2 回移植 |
| 6/5 | 回退捕获、逐轮 State 管线 | 游标模式增量更新 |

**架构文档创作高峰**（在 xm 的 `.trae/documents/` 中）：

| 文档 | 领域 | 决策密度 |
|------|------|----------|
| `pipeline-guard-state-machine.md` | 管线并发控制 | 5 态模型 + 9 变量替换 → 1 集中 guard |
| `state-stm-split-plan.md` | 管线拆分 | State/STM 双轨道 + 信号通知机制 |
| `pipeline-split-and-drain-plan.md` | 排水控制 | 连续性排水避免空 LTM 调用 |
| `stm-three-tier-segmentation-plan.md` | 提取精度 | L1 规则 → L2 词汇 → L3 LLM 递进 |
| `retrieval-v2-plan.md` | 检索架构 | 9 大设计决策 |
| `consolidation-plan.md` | LTM 整合 | 双阈值控制 + 归一化 |
| `injection-wrapper-plan.md` | 注入协议 | 记忆注入包装头协议 |
| `state-self-correction.md` | State 质量 | 多层纠错体系（SP 数据库思想借鉴） |
| `ne-memory-vault-bottom-drawer-plan.md` | UI 重构 | 底部抽屉替代右侧面板 |
| `platform-decoupling-plan.md` | 架构演进 | Core Engine ↔ ST Adapter 解耦 |

---

### 阶段一：0→1 生产级发布（6/6 ~ 6/9）

**仓库位置**：`d:\SillyTavern\xm\ne-memory`（子项目）

| 日期 | 核心活动 | 关键产出 |
|------|----------|----------|
| 6/6 | 管道触发器从 `onMessageSent` 迁移到 `onMessageReceived`；回退捕获防重复；World Book 字段防泄漏 | 11 次 CDN URL 更新（高频迭代） |
| 6/7 | State Schema 全面翻新——三种 prompt 模式、动态角色面板、两段式输出格式强制；Cursor/State 双管线拆分为独立轨道 | 核心架构确立 |
| 6/8 | SmartPush 懒激活 + 全量 dump 回退；5s 硬超时保护；重入守卫阻断级联递归 | `onBeforeGenerateRunning` 守卫 |
| 6/9 | **v1.0.0 发布**：面板全面翻新、Tool-calling 上线、审计 9 bug 全修、快照限制 | 生产级里程碑 |

**关键决策**：
- 从 xm 父项目的"架构设计"模式切换到 ne-memory 的"高频迭代 + CDN 推送"模式
- CDN URL 一天更新 20+ 次，每次修一个 bug 立即推送

---

### 阶段二：深化期 — 管线重构 + World Book 整合（6/10 ~ 6/13）

| 日期 | 核心活动 | 版本 |
|------|----------|------|
| 6/10 | 动态 State 字段发现（角色卡描述/first_mes/世界书自动扫描）、双模式自动切换、`dynamic.*` 路由 | v2.0 |
| 6/11-12 | State Schema Core 层翻新、World Book 记忆整合、LLM 输出验证（缺失字段拒绝+重试） | — |
| 6/13 | **检索 v2 架构决策**（在 xm 父项目中）：BM25 只对 STM / 向量作预筛 / 不做叙事权重标签 / 多视图投影替图遍历 / 笔记本不持久化 / 记忆 LLM 合成保留 | — |

**阶段性特征**：架构设计（xm）和工程实施（ne-memory）两条线并行，xm 倾向于"深度设计"，ne-memory 倾向于"快速实现"。

---

### 阶段三：架构重构期（6/15 ~ 6/25）

| 日期 | 核心活动 | 版本 |
|------|----------|------|
| 6/15-17 | 逐轮 State 管线、动态字段发现、Cursor 位置修复、回退捕获防重复、LLM 输出 checkpoint 验证 | v3.0 / v4.0 |
| 6/18-20 | 对话轮次截断修复（`computeWindowStartMsgId` 字段名错误）、聊天切换面板同步 | — |
| 6/22-25 | 上下文窗口注入层设计、ST merge LLM 调用合并、BANNER 回退推断、LLM 输出 checkpoint 后填充 | — |

---

### 阶段四：架构重构高潮（6/25 ~ 7/3）

| 日期 | 核心活动 | 版本 |
|------|----------|------|
| 6/26 | 上下文窗口记忆注入、三重管线压力系统（token 密度+maxContext+窗口轮数）、physique 字段、NE-CHAR 回退、faction 提取独立 LLM 调用、KV-Cache 优化 | v5.0 |
| 6/27-30 | **Pipeline 拆分**（STM/LTM/State/InnerThoughts 四大独立子管线）、SmartPush 重构、检索与合成分离、Embedding 一键预设、反回声查询策略 | v5.6 |
| 7/1-3 | 死代码清理、Inner Thoughts 管线上线、向量质量测试、v6.0 发布 + v6.1 文档重写 | v6.0 / v6.1 |

**关键突破**：
- Pipeline 从单体到四线程独立：这是架构上最大的一次"解耦"
- SmartPush 的角色从"onBeforeGenerate 中触发 Memory LLM"改为"纯本地 BM25/向量格式化注入"，Mem LLM 专属 `recall_memory` 工具
- KV-Cache 复用让 State LLM prompt 拆为两条 system message，减少了约 40% 的重复 token 消耗

---

### 阶段五：UI 质量 + Schema 系统（7/3 ~ 7/15）

| 日期 | 核心活动 | 版本 |
|------|----------|------|
| 7/3-4 | **Shadow DOM 适配**（CSS 变量穿透失效 → :host 硬编码 → 最终用法达到稳定）、L1/L2/L3 三级 UI 负债清理、4 面板统一滚动架构、字体图标替换、无障碍适配 | v6.1 / v6.2 |
| 7/4-5 | **持久化 Bug 集中修复**：设置面板/记忆编辑/处理历史 3 类静默崩溃 + 滑动关闭 transform 残留、loadVault 版本平局覆盖 | v6.3 |
| 7/5-6 | Extension 模式 + 多通道 API 路由 + 动态摘要比例 + 对数滑块、面板 CSS 三重坑（双滚轮+下滑翻开+占全屏）集中修复 | v6.4 / v6.5 |
| 7/7 | **API 韧性机制**（v1/models 检测+退避重试+Key 格式校验）、STM 合并 LLM 调用（API 调用减 5 倍）、面板 overlay 彻底分层（挂载 `<body>` + `position:fixed`） | v6.6 / v6.7 |
| 7/8 | 角色卡删除、消息接收崩溃修复（import 缺失）、max_tokens 天花板提升 | v6.8 |
| 7/9 | **版本链系统 Day 1**：Open Character Schema Phase 1-5、统一消息 ID（P0）、Delta 版本链（P1）、Pipeline Log、精确回退（P2） | Schema 系统上线 |
| 7/10-11 | Vault 拆分（state_vaults + memory_vaults）、DB 版本迁移 v5→v8、快照系统移除（→版本链替代）、模板 LLM Function Calling 接入 State 管线 | 存储层重构 |
| 7/12 | **Template 系统**：三层锁架构（卡级/全局/系统预设）、势力/任务模板扩展、方案编辑器、NE-CHAR 动态字段 | 模板系统上线 |
| 7/13 | P0+P1 性能优化（检索缓存+浅拷贝+token 效率扫描）、注入格式 HTML 化+相对时间前缀+多样性过滤、外部导航按钮对接 bus | 性能冲刺 |
| 7/14 | **Template 面板统一化重构**（双区卡片布局）、版本链回退修复、生产构建 dev 剥离、面板滚动条美化 | UI 终稿 |
| 7/15 | **收关**：版本链精确回退（删除/重掷/滑动/编辑四种场景）、跨类型字段泄漏修复（validate 拒绝 + prompt 限定）、审计一致性检查、全量重构去重 | 当前 |

---

### 时间投入分布

```
阶段零（架构设计）：     5/28 - 6/5   (  9 天)  13.0%
阶段一（0→1 发布）：     6/6  - 6/9   (  4 天)   5.8%
阶段二（深化期）：       6/10 - 6/13  (  4 天)   5.8%
阶段三（架构重构）：     6/15 - 6/25  ( 11 天)  15.9%
阶段四（重构高潮）：     6/26 - 7/3   (  8 天)  11.6%
阶段五（UI+Schema）：    7/4  - 7/15  ( 12 天)  17.4%
夜间/凌晨加班：          8 次（7/9, 7/10, 7/11, 7/12, 7/13, 7/14, 7/15）
崩盘恢复时间：           1.5 天（NE 插件全阶段恢复，stash 回退吞代码）
```

---

## 二、反思：以什么标准评判？

### 评判维度

| 维度 | 定义 | 权重 |
|------|------|:---:|
| **架构前瞻性** | 初始设计能否支撑后续扩展而不推倒重来？ | 🔴 高 |
| **迭代速度** | 从需求到上线的时间密度 | 🔴 高 |
| **质量防护** | Bug 引入后的发现速度和修复质量 | 🔴 高 |
| **文档习惯** | 计划/决策/代码注释的可追溯性 | 🟡 中 |
| **工具链成熟度** | CDN/构建/测试/发布的自动化水平 | 🟡 中 |
| **知识积累** | 从错误中学习并系统性地避免同类问题 | 🟢 低 |

---

## 三、哪些做得好？

### 3.1 架构前瞻性 ★★★★★

**评判标准**：初始设计能撑多少轮重构而不需要"推倒重来"

**证据**：
- STM/LTM/State 三层架构从 P1-P3 骨架（5/28）确立后，经历了 `State/STM 拆分 → 独立子管线 → 逐轮增量 → Template LLM 接入 → Function Calling` 共 5 轮大规模架构演进，**核心抽象（vault 存储层 + pipeline guard 编排器 + Schema 约束层）从未被推翻**。
- `pipeline-guard-state-machine.md`（6/3）设计的 5 态模型（idle/state/stm/ltm/injecting）经过了 `Pipeline 拆分`（6/30）和 `并行队列替代串行互斥`（7/9），状态机本身仍然有效，只需扩展队列实现。
- `retrieval-v2-plan.md`（6/13）的 9 大设计决策中，"BM25 只对 STM / 向量作预筛 / 不堆叙事权重标签 / 多视图投影替图遍历"这四个核心决策至今仍然成立。

**为何做得好**：
- 在写第一行代码之前花了完整的 9 天做纯架构设计（阶段零）
- 设计文档的"结论+理由"格式让每个决策都有可回溯的 trade-off 记录
- 抽象层边界清晰：`engine` 只管逻辑、`vault` 只管存储、`adapter` 只管 ST 桥接——平台解耦计划（`platform-decoupling-plan.md`）正是基于这个清晰边界

### 3.2 迭代速度 ★★★★★

**评判标准**：从需求到用户可用的时间密度

**证据**：
- 49 天产生 500+ commits（日均 10.2 个提交）
- 版本发布：v0.2→v0.3→v0.4→v1.0→v2.0→v3.0→v4.0→v5.0→v5.6→v6.0→v6.1→v6.2→v6.3→v6.4→v6.5→v6.6→v6.7→v6.8，**平均每 2.7 天发一个版本**
- CDN 分发链路让每个 commit 即时触达用户：`git push → CDN 缓存更新 → 用户刷新` 全链 < 1 分钟

**为何做得好**：
- CDN 分发 + Tampermonkey 脚本装载器的"零安装"模式消除了分发延迟
- 6月6日一天更新了 20+ 次 CDN URL（每修一个 bug 就推送），体现了极致的"修了就发"
- 版本号与 commit 强绑定（manifest.json 为真源），杜绝了版本混乱

### 3.3 文档习惯 ★★★★★

**评判标准**：对话/计划/决策的可回溯性

**证据**：
- xm 父项目 66 篇架构文档 + ne-memory 35 篇实施文档 = **101 篇可追溯的计划/决策记录**
- `PLAN_INDEX.md` + 自动索引脚本，每篇文档带 YAML frontmatter（status / created / updated）
- CHANGELOG.md 从 git log 补全到 18 个版本（从 v0.2 到 v6.8），每条追溯到具体 commit 和根因
- `AGENTS.md` 定义了 grill-me 闸门规则（三档：不触发/提醒/直接执行），按文件风险等级分类
- `.rules.md`（engine + vault 子目录级规则）让 AI 在操作高风险模块时自动读取约束

**为何做得好**：
- 建立了"计划→执行→CHANGELOG"的闭环文档流
- 文档索引自动生成（pre-commit hook），不依赖人工维护
- 计划文档带 frontmatter 状态追踪，而非写完即忘

### 3.4 质量防护的深度 ★★★★

**评判标准**：Bug 发现方式从"用户报告"到"系统自动捕获"的进化

**证据**：
- `computeWindowStartMsgId` import 缺失 → 每条消息崩溃（v6.7→v6.8，0.5 天修复）
- `saveSettingsTab` 静默崩溃导致所有设置不保存 → 空值保护 + 审计全面修复（v6.3）
- `loadVault` 版本平局覆盖 IndexedDB → 严格大于比较（v6.3）
- "11 audit bugs"一次批量修复（v1.0.0）→ 说明建立了主动审计习惯
- 测试体系从 0 到 26/26 green，覆盖 smoke + schema + delta-version + store

**为何做得好**：
- 对"静默失败"类 bug 有高度的敏感性——多次主动排查 `try/catch` 空缺和错误吞噬
- `project_memory.md` 积累了跨会话的教训
- 建立了"发现一个 bug → 排查同类 bug 在整个代码库"的审计思维

---

## 四、哪些不够好？值得优化的部分

### 4.1 父项目→子项目的切换时数据断裂 ★★

**问题**：xm 父项目的 66 篇架构文档和 ne-memory 子项目的 35 篇实施文档之间**没有链接**。检索 v2 的 9 大设计决策在 xm 中，但具体实施在 ne-memory 中，想看完整链路需要跨仓库追踪。

**后果**：
- xm 的 `.trae/documents/` 在 6/13 之后就不再更新（只剩 retrieval-v2-plan 的一次修改），但 ne-memory 持续迭代到 7/15
- 两个 `.trae/` 目录互不知道对方的存在

**改进方案**：
- 在切换子项目时，应该在 ne-memory 的 `PLAN_INDEX.md` 中引用 xm 的架构文档
- 或者在 xm 的文档中标注"已移交 ne-memory 实施"

### 4.2 阶段一的"热修复循环"过高频 ★★

**问题**：6/6 一天更新了 20+ 次 CDN URL（`chore: update CDN URL to xxx`），每次修一个 bug 就立即推送。

**后果**：
- git 历史被大量 CDN URL 更新 commit 污染
- 这些 commit 不携带任何功能信息，纯粹是"同步 dist 产物"

**改进方案**：
- CDN URL 更新应该与功能修复合并提交，或者通过 CI/CD 自动同步
- 或者使用 `--ammend` + force push（在个人分支上）来压缩同一天的 CDN 同步提交

### 4.3 6/27-6/30 的 panel 拆分后 import 缺失 ★★

**问题**：面板代码从一个文件拆分为 6 个模块后，`loadVault`、`isAuto`、`createVaultPopout` 等 11 个跨文件引用缺失。

**后果**：
- 3 个连续提交专门修复 import 缺失（`fix(panel): add missing loadVault import` → `fix(panel): fix missing imports after panel split` → `fix(panel): fix all missing imports after panel split`）
- 如果当时有模块级 lint 规则（`no-undef`），这些错误会在拆分时立刻被发现

**改进方案**：
- 大规模文件拆分前先在 IDE 中运行 lint/typecheck
- 或者先写 import 映射表，确保每个拆分后文件的引用完整

### 4.4 Cascade Loop 问题的发现方式 ★★

**问题**：`generateRaw`/`generateQuietPrompt` 内部调用 ST 的 `Generate()` 触发 `GENERATION_AFTER_COMMANDS`，形成递归级联。这个问题经历了 5 个 commit 的排查链：

```
diag: add MutationObserver → fix: make spurious trigger guard true no-op
→ fix: add spurious trigger guard → fix: add re-entrancy guard
→ fix: cascade loop (最终修复)
```

**后果**：前 4 个 commit 是诊断性探针和临时补丁，直到第 5 个才找到根因。

**改进方案**：
- 对于调用外部 API（ST 的 `Generate()`、`generateRaw` 等）的函数，应该从一开始就假设它们可能触发级联调用
- 建立"级联调用检测模式"：任何在事件回调中调用可能触发同事件的外部 API 的地方，都应该有重入守卫

### 4.5 Shadow DOM CSS 问题多轮试错 ★★

**问题**：v6.1 引入 Shadow DOM 后，CSS 变量穿透失效导致了一系列问题，修复路径非常曲折：

```
CSS 变量重注入 → :host 选择器 → 移除 CSS 变量改用 :host 硬编码
→ 字体图标不可见 → Unicode 替换 → Font Awesome 替换
→ API 状态圆点 Unicode → FA → 纯 CSS 圆点 → 硬编码颜色
```

**后果**：UI 层花费了约 3 天时间在 Shadow DOM 适配上（v6.1~v6.2 期间），但核心功能开发被延迟。

**改进方案**：
- 在采用 Shadow DOM 之前，先在一个独立测试页面验证所有 CSS 策略
- 或者考虑是否真的需要 Shadow DOM——如果你在 iframe 中渲染（TH 环境），Shadow DOM 是额外的隔离层，可能可以用更轻量的 scoped CSS 策略替代

### 4.6 凌晨加班模式 ★★★

**问题**：日志显示 8 个凌晨窗口（7/9 02:19、7/10 多个夜间提交、7/12、7/13、7/14 均有凌晨提交）。

**后果**：
- 凌晨的代码质量可能低于白天（多个凌晨 commit 紧随其后的是 bug 修复）
- `NE 插件全阶段恢复计划`——`publish-extension.cjs` 的 `git stash push` 吞掉了 src/ 修改，需要从 release 分支恢复代码。这很可能发生在夜间操作中，白天的注意力可能更容易注意到 stash 会清空工作区。

**改进方案**：
- 对可能破坏工作区的命令（`git stash push`、`git reset --hard`、`git clean -f`）建立"操作前备份"习惯
- 或者改成 CI/CD 自动化发布，消除手动操作带来的风险

---

## 五、如果再来一次，什么地方可以做得更好？

### 5.1 第一天就做版本链

**现状**：快照系统（`saveVaultWithSnapshot` 深拷贝 50-200KB 的全量数据）从 v1.0 用到 v6.8，然后在 7/9 才被 Delta 版本链替代。

**如果重来**：
- 在 v2.0（6/6）确立 State Schema 抽象后，就应该意识到"每次全量快照是不可持续的"
- Delta 版本链的设计并不复杂（160 字节~1.5KB 的 diff），如果在一开始就实现，后续 30 多个版本的存储压力和回滚精度都会好很多
- 这也避免了"先建快照系统→再移除→再建版本链"的重构开销

### 5.2 提前建立"错误吞噬审计"机制

**现状**：多个严重 bug 的共同特征是"try/catch 中错误被静默吞噬"——`saveSettingsTab` 的 `value.trim()` TypeError、`restoreSnapshot` 异步回调错误、`collectAllMsgIds` import 缺失。这些 bug 都是在用户反馈后才发现的。

**如果重来**：
- 建立一个 `safeCall(fn, context)` 包装器，确保任何静默失败都有日志记录
- 集成 Sentry 或类 Sentry 的错误收集（Telemetry Issue Report 已经有了，但需要更主动的推送）
- 把 `try { ... } catch (e) { /* empty */ }` 设为 lint 禁止规则

### 5.3 先跑测试再拆分大文件

**现状**：panel 从 1 个文件拆为 6 个模块时，import 缺失导致了 3 个 follow-up fix 提交。测试套件（26/26）是在事后补的。

**如果重来**：
- 大规模文件拆分前，先建立最小测试覆盖（至少 smoke test 能跑通）
- 拆分后立即运行 lint + 测试，而非依赖人工检查
- 实际上你的测试套件已经很强了（252 asserts, 4 files），但它是在代码写完之后补的——如果能在每次架构变更时增量加测试，拆分引入的回归会立即暴露

### 5.4 用一个窗口完成一个完整功能周期

**现状**：Trae 对话记忆显示，不同话题散落在不同窗口中。商业策略讨论（7/11）和 Schema 开发（7/8-7/15）可能不在同一个上下文中。

**如果重来**：
- 每个功能模块规定"一个窗口完成设计→实施→测试→文档"，减少跨窗口的上下文丢失
- 利用 `TOPIC_CHANGE` 明示来触发 AI 的上下文切换，而非隐式地开新窗口
- 对话记忆中的 `topics.md` 和 `project_memory.md` 已经有了——但它们更多是"事后摘要"，而非"进行中的追踪"

### 5.5 给父项目和子项目建立层级关系

**现状**：xm 是父项目（SillyTavern extension 的容器），ne-memory 是子项目（实际工程的仓库）。但两个 `.trae/` 目录各管各的，文档索引也不互通。

**如果重来**：
- 用一个顶层的 `PROJECT_MAP.md` 描述容器内的所有子项目及其关系
- 在 ne-memory 中引用 xm 的架构文档（"设计依据来自父项目的 `retrieval-v2-plan.md`"）
- 让两个 `.trae/` 目录共享一个 `PLAN_INDEX.md` 入口

---

## 六、总结

### 关键数字

| 指标 | 数值 |
|------|------|
| 总开发天数 | 49 天 |
| 总提交数 | 500+ |
| 版本数 | 18 个（v0.2 → v6.8） |
| 架构设计文档 | 66 篇（xm 父项目） |
| 实施计划文档 | 35 篇（ne-memory 子项目） |
| 日均版本 | 0.37 版/天（约每 2.7 天一个版本） |
| 日均提交 | 10.2 次/天 |
| 凌晨工作窗口 | 8 次 |
| 测试覆盖 | 26/26 green, 252 asserts, 4 files |

### 核心优势

1. **架构前瞻性**：三层架构（STM/LTM/State）+ pipeline guard 抽象层，支撑了 5 轮大规模重构而不需推倒重来
2. **迭代速度**：CDN 零安装分发 + 每日多次推送，49 天完成从零到生产级
3. **文档可追溯**：101 篇计划/决策文档 + 自动索引 + YAML frontmatter 状态追踪，实现了"每一个 commit 都能找到它的设计来源"

### 核心改进空间

1. **存储抽象延迟**：版本链系统如果在 v2.0 就引入，而非 v6.8 才替代快照系统，可以节省大量重构开销
2. **错误可见性**：静默失败的 bug 占比过高——提前建立错误吞噬审计机制可以大幅减少用户发现 bug 的次数
3. **跨仓库协调**：xm 和 ne-memory 的文档/设计/实施链路如果能建立显式的引用关系，会减少"设计在某处、实现在别处、谁也找不到谁"的信息断裂
4. **夜间代码质量**：凌晨高强度的 commit 直接导致了一些需要事后多轮修复的问题——CI/CD 自动化可以减少对深夜手动操作的依赖
